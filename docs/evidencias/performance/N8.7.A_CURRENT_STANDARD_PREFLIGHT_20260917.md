# N8.7.A — Performance — current-standard preflight

Authority: `docs/VAEP_AUTHORITY.md` (blob `abbc1bcc9e4f5fb2de2468025e20c44566818a5f`)
Inspected branch: `Desarrollo`
Inspected functional/control baseline: `1e7c489b47f68cc039b3e97e991c517151feceef`
Revalidated at: `2026-09-17T10:48:12Z`

## Dependency and admission

- `GATE-N7` is fresh-current-standard `LISTO` in the operational control plane and therefore satisfies the dependency for N8.7.A.
- N8.7.A was re-opened by the owner-ordered current-standard revalidation; historical N8.7 receipts are retained only as history and are not used as current closure evidence.
- The prior N8.7.A lease was superseded only after physical-liveness readback showed that the previous automation invocation had ended; a recent heartbeat by itself was not treated as `ACTIVE_REAL`.
- Single-writer ownership is held by Tarea Supervisión :36 under token `SUP36-N87A-TAKEOVER-20260917T104244Z`.

## Scope

N8.7 verifies performance in Development through controlled, authenticated, tenant-scoped load/stress/concurrency/endurance/volume exercises over representative API flows. Measurements are observations, not invented production SLAs.

In scope for the N8.7 chain:

- authenticated Development-only traffic using synthetic/test data;
- representative read/write flows selected before the load run;
- controlled concurrency and volume with explicit stop conditions;
- response-time/error-rate/resource observations and causal evidence;
- tenant isolation, authorization and audit safety while load is generated;
- cleanup/reconciliation of synthetic data after the controlled run.

Out of scope:

- Production or production data;
- `main`, PR #2 merge/auto-merge, DNS, certificates, secrets or production infrastructure;
- destructive schema/data operations;
- speculative indexes, caches or product changes before a measured bottleneck justifies them;
- fabricated SLO/SLA claims.

## Current-head inspection

Targeted current-repository searches did not find a dedicated N8.7 load harness or common load-generator definitions (`k6`, Artillery, JMeter, Gatling, wrk/vegeta/locust equivalents). This is not a blocker for PRE; it means the actual controlled harness/run belongs to the later TEST_CI stage and must be materialized or executed there before N8.7.G can close.

No product/domain/schema/frontend delta is required merely to define this preflight. Later stages must not invent such deltas without causal evidence.

## Risks and mandatory guards

- Provider/service quota exhaustion: ramp gradually; stop on provider throttling or unsafe saturation.
- Database/resource pressure: Development only; monitor health and stop before risking persistent corruption.
- Tenant leakage/authorization regression: synthetic tenant-scoped actors only; any cross-tenant access is immediate fail/stop.
- Side effects: use reversible test fixtures and reconcile/clean generated data.
- Timing noise: record exact test profile and measured observations; do not convert one run into an unqualified production SLA.
- Security: no secret values in logs/evidence; only secret-name/config-presence checks where needed.

## Acceptance and rollback

N8.7.A PRE is satisfied when scope/out-of-scope, dependency, risks, safety guards, rollback/cleanup and later-stage test obligations are explicit and current. This PRE itself changes no runtime code, schema, data or infrastructure, so rollback is removal/reversion of this evidence-only documentation/control-plane closure. Any later load execution must stop generators and clean/reconcile synthetic data as its operational rollback.

Current PRE assessment: `PASS`, P0=0, P1=0. The material performance proof remains intentionally deferred to N8.7.G and cannot be inferred from this document.
