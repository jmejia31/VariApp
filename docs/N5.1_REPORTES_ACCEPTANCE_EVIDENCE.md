# N5.1 — Centro de Reportes: evidencia de aceptación

## Alcance de esta reconciliación

Este documento corresponde a `N5.1.G.2.ACCEPTANCE_MATRIX_RECONCILIATION` y fue materializado por QA_TAKEOVER después de que el ATTEMPT1 remoto agotó su lane budget sin patch. La autoridad es `docs/VAEP_AUTHORITY.md`; esta evidencia no declara `LISTO_REAL` ni inventa resultados no ejecutados.

Head de trabajo al iniciar QA_TAKEOVER: `126e7c286190083c4734c2eeccd5e34b20dc47dd`.
Cambio E2E causal añadido durante el takeover: `fc609189d42bc5a7162b5ccda77d50f917efc76e`.

## Matriz reconciliada contra implementación viva

| ID | Estado | Evidencia observable / causal | Pendiente para certificar |
| --- | --- | --- | --- |
| A1 | CUBIERTO_POR_CONTRATO / EJECUCIÓN_PENDIENTE | `ReportesAdministrativosController` está protegido por `[Authorize]`; las rutas viven bajo `reportes-administrativos`. | Ejecutar suite causal sobre exact HEAD para registrar 401. |
| A2 | CUBIERTO_POR_E2E / EJECUCIÓN_PENDIENTE | `frontend/e2e/fase6-reportes-administrativos.spec.ts` crea rol limitado sin `ReportesAdministrativos:Ver` y exige 403 en rutas administrativas. | Resultado causal del workflow/test. |
| A3 | CUBIERTO_POR_BACKEND_CONTRACT / EJECUCIÓN_PENDIENTE | El controlador exige además `_currentUser.EsAdministrador`; un usuario sin ese guard no puede consumir las lecturas administrativas. | Ejecución causal específica si el gate la requiere. |
| A4 | CUBIERTO_POR_E2E / EJECUCIÓN_PENDIENTE | La suite consulta resumen, usuarios/accesos y roles/permisos con administrador; el Centro de Reportes también navega al child administrativo. | Resultado causal del workflow/test. |
| A5 | PARCIAL / GAP_EXPLÍCITO | Existe permiso `ReportesAdministrativos:Exportar` y el contrato del controlador lo exige. La suite actual valida 403 de lectura para rol limitado, no un fixture con `Ver` permitido y `Exportar` denegado. | Fixture de permiso granular o prueba backend dedicada. |
| A6 | CUBIERTO_POR_E2E / EJECUCIÓN_PENDIENTE | La suite exporta CSV/XLSX, valida MIME/contenido y que no se filtren credenciales; el controlador registra auditoría de exportación. | Resultado causal del workflow/test y, si se exige, aserción persistida de auditoría. |
| B1 | CUBIERTO_POR_E2E_BÁSICO / EJECUCIÓN_PENDIENTE | `resumen` se invoca con `desde=hoy&hasta=hoy`. | Resultado causal. |
| B2 | IMPLEMENTADO / TEST_CAUSAL_PENDIENTE | `ReporteAdministrativoService.NormalizarPeriodo(...)` rechaza fail-closed cuando `Hasta < Desde` mediante `BusinessRuleException`. | Ejecutar prueba causal específica; no requiere implementación adicional mientras el contrato permanezca vigente. |
| B3 | IMPLEMENTADO / TEST_CAUSAL_PENDIENTE | `NormalizarPeriodo(...)` aplica ventana por defecto de ~30 días y rechaza períodos mayores de 366 días; la exportación de auditoría además limita materialización a `Take(50_000)`. | Ejecutar prueba causal del límite de 366 días y mantener E5 separado para volumen de exportación. |
| B4 | PARCIAL | Varias rutas administrativas se invocan sin filtros opcionales en la suite. | Registrar ejecución causal y confirmar comportamiento determinista donde aplique. |
| C1 | GAP | No se declara PASS de loading sin prueba observable causal. | Prueba UI dedicada si el componente de datos expone loading. |
| C2 | GAP | No se declara PASS de empty-state de datos administrativos sin fixture vacío controlado. | Fixture y aserción UI. |
| C3 | GAP | No se declara PASS de error HTTP/transport sin intercept/fixture causal. | Prueba UI dedicada. |
| C4 | CONSERVAR_REGRESIÓN | El preflight identifica la protección frente a stale responses como evidencia existente que debe mantenerse. El takeover no elimina esa cobertura. | Ejecutar la prueba causal existente correspondiente. |
| D1 | GAP / NO_APLICAR_COMO_PASS | No existe evidencia nueva de fixture histórico que demuestre inmutabilidad frente a edición de catálogos. | Fixture histórico causal. |
| D2 | GAP / NO_APLICAR_COMO_PASS | No existe evidencia nueva de soft-delete histórico. | Fixture causal con entidad inactiva/eliminada lógicamente. |
| D3 | N/A_PARA_REPORTES_ADMIN_ACTUALES | El contrato observable de `ReportesAdministrativosController` no expone reporte de costo/utilidad. | Reabrir solo si una faceta futura incorpora ese reporte. |
| E1 | EJECUCIÓN_PENDIENTE | La suite existente fue extendida, no reemplazada, con navegación directa `/centro-reportes` -> `/centro-reportes/administrativos`. | CI/test causal sobre exact HEAD funcional. |
| E2 | PASS_POR_SCOPE_DELTA | QA_TAKEOVER modifica únicamente prueba E2E y esta evidencia; no cambia Ventas, Compras, Inventario, persistencia ni contratos transaccionales. | Ninguno mientras el diff permanezca acotado. |
| E3 | PASS_POR_INSPECCIÓN_DE_SCOPE | El delta no añade mutaciones de dominio; la única escritura ya reconocida en el contrato sigue siendo auditoría explícita de exportación. | Validación causal del controlador si cambia el producto. |
| E4 | GAP | No existe presupuesto medible aprobado en esta reconciliación. | Benchmark/SLI aprobado antes de declararlo PASS. |
| E5 | GAP | Existe límite de 50.000 filas solo para exportación de auditoría, pero no una política certificada general de paginación/streaming/asincronía para exportaciones grandes de todos los tipos. | Decisión de contrato y prueba causal antes de PASS. |

## Cambio E2E material del QA_TAKEOVER

La suite `frontend/e2e/fase6-reportes-administrativos.spec.ts` conserva toda la cobertura previa y agrega una regresión explícita del Centro de Reportes independiente:

1. login de administrador;
2. navegación directa a `/centro-reportes`;
3. verificación del heading `Centro de Reportes` y nav accesible;
4. navegación por el enlace `Reportes administrativos`;
5. verificación de URL `/centro-reportes/administrativos`;
6. verificación de que el contenido administrativo reutilizado se renderiza;
7. comprobación de ausencia de overflow horizontal.

La prueba legacy `/auditoria` se conserva y se renombra descriptivamente para dejar claro que no fue sustituida.

## Tests / gates

No se declara ningún comando como PASS hasta que GitHub Actions o una ejecución causal sobre el exact HEAD funcional lo demuestre. El gate preferido para esta faceta es la suite Playwright que contiene `frontend/e2e/fase6-reportes-administrativos.spec.ts` y cualquier CI causal asociado al commit de QA_TAKEOVER.

## Riesgos y limitaciones

B2 y B3 dejan de clasificarse como gaps de implementación: el código vivo ya contiene ambas reglas y lo pendiente es evidencia de ejecución causal. Permanecen abiertos A5, C1, C2, C3, D1, D2, E4 y E5; B4 y otras filas de ejecución siguen pendientes de prueba causal. Ninguno puede convertirse en PASS por inferencia documental.

## SELF_REVIEW_PASS_1

PASS. Se volvió a contrastar la matriz contra `ReporteAdministrativoService.NormalizarPeriodo(...)`, `ReportesAdministrativosController`, la UI y la suite E2E vivas. Se corrigió la clasificación falsa de B2/B3 sin inventar resultados de test.

## SELF_REVIEW_PASS_2

PASS. Se verificó que la reconciliación conserva explícitos los gaps reales, mantiene separada la política de rango (B3) del riesgo de exportación grande (E5), no toca manifests, secretos, Producción ni `main`, y no declara `LISTO_REAL` ni CI no ejecutado.
