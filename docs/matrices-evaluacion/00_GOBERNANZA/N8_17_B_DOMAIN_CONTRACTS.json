{
  "schema": "vaep-n8.17-domain-contracts/v1",
  "parent": "N8.17.B",
  "branch": "Desarrollo",
  "source_manifest": "docs/matrices-evaluacion/00_GOBERNANZA/N8_17_A_BATCH_MANIFEST.json",
  "expected_total": 49,
  "contract_level": "ROOT_BUSINESS_CONTRACT",
  "notes": "These are root-level business contracts for the 49 frozen N8.16 roots. N8.17.C-G enrich data/API/UX/security/test detail and may add child MATRIX_ID entries after materiality review without weakening these root contracts.",
  "global_invariants": [
    "Backend and persistence remain authoritative for business mutations; client state never grants authority by itself.",
    "Authenticated internal flows are tenant-scoped and permission-scoped; cross-tenant access must fail closed.",
    "A failed validation or authorization path must not leave a partial business mutation.",
    "Retries must not silently duplicate the same logical mutation; concrete endpoint idempotency is refined in N8.17.D."
  ],
  "contracts": [
    {
      "matrix_id": "VAEP-MX::IDENTITY_ACCESS::LOGIN",
      "domain": "IDENTITY_ACCESS",
      "objective": "Authenticate an eligible user and establish an application session without disclosing credential or account-state secrets.",
      "actors": ["Unauthenticated user", "Authentication backend"],
      "preconditions": ["Login surface is reachable", "Submitted credentials are processed only by the authoritative authentication path"],
      "invariants": ["Authentication fails closed", "Invalid credentials do not reveal whether a principal exists", "Successful identity is the only source for subsequent authorization"],
      "happy_path": ["User submits credentials", "Backend validates identity and account eligibility", "A valid authenticated session/token is established", "User proceeds to an authorized application surface"],
      "alternate_flows": ["Invalid credentials return a generic denial", "Inactive or unauthorized account is denied", "Transient authentication failure leaves the user unauthenticated"],
      "side_effects": ["Authentication/session state may be created or refreshed", "Security/audit telemetry may record the attempt without secrets"],
      "idempotency": "Repeating a rejected attempt creates no business mutation; successful re-authentication may replace or refresh session state but must not create duplicate business records."
    },
    {
      "matrix_id": "VAEP-MX::IDENTITY_ACCESS::PERFIL",
      "domain": "IDENTITY_ACCESS",
      "objective": "Allow an authenticated user to view and maintain the profile attributes that policy permits them to change.",
      "actors": ["Authenticated user", "Identity/profile backend"],
      "preconditions": ["Authenticated principal exists", "Requested profile belongs to the active principal or an explicitly authorized administrative context"],
      "invariants": ["Identity ownership cannot be changed through profile presentation state", "Protected security attributes require their dedicated authority", "Tenant and principal boundaries remain enforced"],
      "happy_path": ["Load current profile", "User edits permitted fields", "Backend validates and persists accepted changes", "Readback shows authoritative profile"],
      "alternate_flows": ["Invalid field values are rejected without mutation", "Forbidden fields remain read-only", "Concurrent or missing profile state returns a controlled error"],
      "side_effects": ["Permitted profile fields may change", "Mutation may create audit metadata"],
      "idempotency": "Submitting the same accepted profile values repeatedly converges to the same stored state."
    },
    {
      "matrix_id": "VAEP-MX::IDENTITY_ACCESS::PERMISOS",
      "domain": "IDENTITY_ACCESS",
      "objective": "Administer the permission matrix that controls module/action authorization without granting privileges from client-only state.",
      "actors": ["Authorized security administrator", "Authorization backend"],
      "preconditions": ["Administrator is authenticated", "Administrator has the explicit permission-administration authority"],
      "invariants": ["Authorization decisions are enforced server-side", "Permission changes remain tenant-scoped", "A caller cannot self-escalate outside granted administrative authority"],
      "happy_path": ["Load authoritative permission assignments", "Administrator changes an allowed assignment", "Backend validates authority and target", "Assignment is persisted and read back"],
      "alternate_flows": ["Unauthorized changes are rejected", "Unknown module/action targets are rejected", "Conflicting or stale changes fail without partial privilege mutation"],
      "side_effects": ["Role/user permission assignments may change", "Security audit trail records effective authorization changes"],
      "idempotency": "Applying the same assignment state repeatedly yields one effective permission state, not duplicate grants."
    },
    {
      "matrix_id": "VAEP-MX::IDENTITY_ACCESS::ROLES",
      "domain": "IDENTITY_ACCESS",
      "objective": "Manage tenant-scoped roles and their authorization composition as reusable access-control units.",
      "actors": ["Authorized security administrator", "Authorization backend"],
      "preconditions": ["Authenticated administrative principal", "Role-management permission is granted"],
      "invariants": ["Role identity and tenant ownership remain stable", "Role edits cannot bypass permission authority", "Referenced permissions must be valid"],
      "happy_path": ["List or open a role", "Create or edit allowed role attributes and assignments", "Backend validates constraints", "Persist and read back the role"],
      "alternate_flows": ["Duplicate or invalid role definitions are rejected", "Forbidden edits are rejected", "Deletion/deactivation with protected dependencies follows explicit policy rather than client assumption"],
      "side_effects": ["Role metadata and permission mappings may change", "Effective authorization can change for assigned users"],
      "idempotency": "Repeated application of the same role definition converges to one role state and one set of assignments."
    },
    {
      "matrix_id": "VAEP-MX::IDENTITY_ACCESS::USUARIOS",
      "domain": "IDENTITY_ACCESS",
      "objective": "Administer application users, their lifecycle and allowed access associations within the active tenant.",
      "actors": ["Authorized user administrator", "Identity backend"],
      "preconditions": ["Authenticated administrator", "User-management authority is granted"],
      "invariants": ["Tenant ownership cannot be forged", "Credential/security material is never treated as ordinary editable profile data", "Privilege assignment remains separately authorized"],
      "happy_path": ["List or open a tenant user", "Create or edit permitted account attributes", "Backend validates uniqueness and policy", "Persist and read back authoritative account state"],
      "alternate_flows": ["Duplicate identity attributes are rejected", "Forbidden privilege changes fail closed", "Invalid lifecycle transitions do not partially persist"],
      "side_effects": ["User account metadata/lifecycle may change", "Authorization associations may be updated only through authorized paths", "Audit events may be emitted"],
      "idempotency": "Retrying the same desired user state must not create duplicate principals or duplicate role mappings."
    },
    {
      "matrix_id": "VAEP-MX::PRODUCT_CATALOG::CATALOGOS_PRODUCTO",
      "domain": "PRODUCT_CATALOG",
      "objective": "Maintain reusable product reference catalogs such as brands, models, colors and sizes used by product definitions.",
      "actors": ["Authorized catalog operator", "Catalog backend"],
      "preconditions": ["Authenticated tenant context", "Relevant catalog view/create/edit permission"],
      "invariants": ["Reference values remain tenant-scoped", "Duplicate logical catalog values are prevented according to authoritative normalization", "Referenced values are not silently removed while in protected use"],
      "happy_path": ["Load a selected catalog", "Create or edit a permitted value", "Backend validates identity and constraints", "Persist and refresh the catalog"],
      "alternate_flows": ["Duplicate or invalid values are rejected", "Forbidden mutation is rejected", "Protected referenced values follow explicit deactivate/delete policy"],
      "side_effects": ["Reference catalog values may change", "Products using those references may display the updated authoritative label/state"],
      "idempotency": "Repeating the same desired reference value update converges to one catalog value."
    },
    {
      "matrix_id": "VAEP-MX::PRODUCT_CATALOG::CATEGORIAS",
      "domain": "PRODUCT_CATALOG",
      "objective": "Manage product categories used to classify and navigate the tenant product catalog.",
      "actors": ["Authorized catalog operator", "Catalog backend"],
      "preconditions": ["Authenticated tenant context", "Category permission for the requested action"],
      "invariants": ["Category identity remains tenant-scoped", "Hierarchy/references cannot point to invalid or cross-tenant categories", "Backend validates all mutations"],
      "happy_path": ["List categories", "Create or edit an allowed category", "Backend validates constraints and relationships", "Persist and read back category state"],
      "alternate_flows": ["Duplicate/invalid category is rejected", "Forbidden mutation fails closed", "Referenced category deletion follows protected dependency policy"],
      "side_effects": ["Category classification/navigation may change", "Dependent product presentation may reflect the updated category"],
      "idempotency": "The same category update repeated produces one authoritative category state."
    },
    {
      "matrix_id": "VAEP-MX::PRODUCT_CATALOG::PRODUCTOS",
      "domain": "PRODUCT_CATALOG",
      "objective": "Manage sellable product definitions and their catalog-facing attributes while preserving inventory and accounting boundaries.",
      "actors": ["Authorized product operator", "Product backend"],
      "preconditions": ["Authenticated tenant context", "Product permission for the requested action", "Referenced catalog entities are valid"],
      "invariants": ["Product identity and tenant ownership are server-authoritative", "Commercial/catalog attributes cannot directly forge inventory movements", "Referenced category/catalog values must exist in the same tenant"],
      "happy_path": ["List or open a product", "Create/edit permitted product data", "Backend validates references and uniqueness", "Persist and read back authoritative product"],
      "alternate_flows": ["Invalid references or duplicate business keys are rejected", "Forbidden action fails closed", "Protected products follow explicit lifecycle policy rather than silent deletion"],
      "side_effects": ["Product catalog data/variants may change", "Downstream sales/purchase presentation may observe the new product state"],
      "idempotency": "Retrying the same desired product state must not create duplicate products or variants."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::CLIENTES",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Manage customer master data used by quotations, orders, invoices and receivables.",
      "actors": ["Authorized commercial operator", "Customer backend"],
      "preconditions": ["Authenticated tenant context", "Customer permission for the requested action"],
      "invariants": ["Customer identity is tenant-scoped", "Sensitive/customer data follows authorized visibility", "Referenced commercial documents keep stable customer linkage"],
      "happy_path": ["List or open customers", "Create/edit permitted master data", "Backend validates business keys and fields", "Persist and read back customer"],
      "alternate_flows": ["Duplicate/invalid customer data is rejected", "Forbidden access fails closed", "Protected customers with dependencies are not silently destroyed"],
      "side_effects": ["Customer master data may change", "Dependent commercial workflows may consume updated customer information"],
      "idempotency": "Repeating the same customer mutation converges to one customer record and does not duplicate it."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::COSTOS_ENVIO",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Administer shipping-cost rules used by billing/checkout calculations under server authority.",
      "actors": ["Authorized billing administrator", "Commercial backend"],
      "preconditions": ["Authenticated tenant context", "Facturacion administration authority"],
      "invariants": ["Shipping cost applied to a transaction is server-validated", "Rules remain tenant-scoped", "Client-provided totals cannot override authoritative calculation"],
      "happy_path": ["Load shipping-cost configuration", "Create/edit an allowed rule", "Backend validates rule consistency", "Persist and expose authoritative configuration"],
      "alternate_flows": ["Overlapping/invalid rules are rejected according to backend constraints", "Forbidden mutation is rejected", "Unavailable configuration produces controlled fallback/error behavior rather than client invention"],
      "side_effects": ["Future shipping calculations may change", "Configuration audit metadata may change"],
      "idempotency": "Applying the same shipping rule configuration repeatedly produces one effective rule set."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::COTIZACIONES",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Create and manage customer quotations that capture proposed commercial terms before order/invoice commitment.",
      "actors": ["Authorized sales operator", "Commercial backend"],
      "preconditions": ["Authenticated tenant context", "Customer/product references are valid", "Quotation action is permitted"],
      "invariants": ["Totals and commercial rules are revalidated server-side", "Quotation ownership is tenant-scoped", "Conversion to downstream documents must preserve traceability"],
      "happy_path": ["Create or open quotation", "Select customer/items and commercial terms", "Backend validates and calculates", "Persist quotation and expose authoritative result"],
      "alternate_flows": ["Invalid items/customer/terms are rejected", "Stale or forbidden transitions fail without partial downstream creation", "Expired/cancelled state follows explicit lifecycle rules"],
      "side_effects": ["Quotation and line state may be created/updated", "Later conversion may create a traceable order/invoice through its own authoritative path"],
      "idempotency": "A retry of the same logical quotation save/conversion must not create duplicate downstream documents."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::DESCUENTOS",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Manage discount definitions and eligibility rules used in commercial pricing.",
      "actors": ["Authorized commercial administrator", "Pricing/commercial backend"],
      "preconditions": ["Authenticated tenant context", "Discount-management permission"],
      "invariants": ["Discount application is server-authoritative", "Discounts remain within configured business bounds", "Tenant isolation applies to definitions and use"],
      "happy_path": ["List discounts", "Create/edit an allowed discount rule", "Backend validates rule scope and values", "Persist and read back"],
      "alternate_flows": ["Invalid percentages/amounts/scopes are rejected", "Forbidden mutations fail closed", "Conflicting eligibility does not silently override authoritative precedence"],
      "side_effects": ["Future pricing calculations may change", "Discount audit/configuration state may change"],
      "idempotency": "Repeating the same discount rule update converges to one rule state."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::FACTURAS",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Issue and manage customer invoices as authoritative billing documents with traceable totals, status and payments.",
      "actors": ["Authorized billing operator", "Billing backend"],
      "preconditions": ["Authenticated tenant context", "Billing action is authorized", "Referenced customer/items/tax context are valid"],
      "invariants": ["Totals, taxes and status transitions are server-calculated/validated", "Issued documents retain tenant and audit traceability", "Client state cannot fabricate payment or cancellation authority"],
      "happy_path": ["Create/open invoice", "Provide permitted billing inputs", "Backend validates and calculates authoritative amounts", "Persist/issue according to lifecycle and return readback"],
      "alternate_flows": ["Invalid references/totals are rejected", "Forbidden lifecycle transition fails closed", "Cancellation/payment changes use explicit authorized paths"],
      "side_effects": ["Invoice/lines/status may change", "Receivable/payment/accounting side effects may be triggered only by their authoritative business paths"],
      "idempotency": "Retrying issue/payment-linked operations must not create duplicate invoices or duplicate financial effects for the same logical request."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::PEDIDOS_VENTA",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Manage sales orders from commercial commitment through fulfillment handoff while preserving inventory and billing traceability.",
      "actors": ["Authorized sales operator", "Sales-order backend"],
      "preconditions": ["Authenticated tenant context", "Customer/items are valid", "Sales-order action is authorized"],
      "invariants": ["Order totals and lifecycle are backend-authoritative", "Order lines remain tenant-scoped and traceable", "Fulfillment/billing transitions cannot be forged by presentation state"],
      "happy_path": ["Create/open order", "Capture customer/items and permitted terms", "Backend validates/calculates", "Persist order and expose authoritative lifecycle state"],
      "alternate_flows": ["Invalid stock/commercial references return controlled validation", "Forbidden or stale transitions fail", "Cancellation/fulfillment conflicts do not partially mutate"],
      "side_effects": ["Sales order and lines may change", "Authorized downstream preparation/invoice flows may be enabled with traceability"],
      "idempotency": "Retries of create/transition operations must not duplicate an order or repeat the same downstream transition."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::TIPO_CLIENTES",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Manage customer-type classification used by commercial policies and reporting.",
      "actors": ["Authorized customer administrator", "Customer backend"],
      "preconditions": ["Authenticated tenant context", "Customer-type permission"],
      "invariants": ["Types are tenant-scoped", "Referenced types are not silently removed while protected", "Business use relies on authoritative type identity rather than display label"],
      "happy_path": ["List types", "Create/edit permitted type", "Backend validates constraints", "Persist and refresh"],
      "alternate_flows": ["Duplicate/invalid type is rejected", "Forbidden action fails closed", "Deletion with dependencies follows explicit policy"],
      "side_effects": ["Customer classification options may change", "Dependent customer/report views may reflect updated type metadata"],
      "idempotency": "Repeated submission of the same type definition produces one effective type."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::VARISTOREHN",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Provide the public/customer storefront journey for browsing products, cart, checkout and order status without exposing internal authority.",
      "actors": ["Storefront visitor/customer", "Storefront backend"],
      "preconditions": ["Public storefront context is resolvable", "Only published/eligible catalog data is exposed"],
      "invariants": ["Public client input never overrides authoritative price, stock, tax, shipping or payment rules", "Internal-only data is not exposed", "Order/customer access tokens or identifiers cannot cross authorization boundaries"],
      "happy_path": ["Browse eligible catalog", "Build cart", "Submit checkout/customer inputs", "Backend revalidates all commercial values and creates/updates the permitted order flow", "Customer receives sanitized status/result"],
      "alternate_flows": ["Unavailable/invalid items are rejected or refreshed", "Payment/checkout failure leaves no fabricated success", "Unauthorized order lookup is denied"],
      "side_effects": ["Cart/client state may change", "Authorized checkout may create a customer/order/payment initiation through server-controlled paths"],
      "idempotency": "Checkout retries reuse logical request identity where supported and must not duplicate the same order/payment intent."
    },
    {
      "matrix_id": "VAEP-MX::CUSTOMERS_COMMERCIAL::VENTAS",
      "domain": "CUSTOMERS_COMMERCIAL",
      "objective": "Record and manage sales transactions with authoritative commercial totals and traceable inventory/financial effects.",
      "actors": ["Authorized sales operator", "Sales backend"],
      "preconditions": ["Authenticated tenant context", "Sales permission", "Customer/product/payment references required by the transaction are valid"],
      "invariants": ["Totals/discounts/taxes are server-authoritative", "Tenant and document ownership are enforced", "Inventory/payment/accounting effects occur only once per accepted sale lifecycle"],
      "happy_path": ["Create/open sale", "Capture permitted items and terms", "Backend validates and calculates", "Persist transaction and return authoritative status"],
      "alternate_flows": ["Invalid stock/price/customer/payment inputs are rejected", "Forbidden/stale transition fails closed", "Cancellation/reversal follows explicit compensating policy"],
      "side_effects": ["Sale and lines may change", "Inventory, receivable/payment or accounting effects may be triggered through authorized transactional boundaries"],
      "idempotency": "Retrying the same logical sale operation must not duplicate the sale or its inventory/financial side effects."
    },
    {
      "matrix_id": "VAEP-MX::PURCHASES_SUPPLIERS::COMPRAS",
      "domain": "PURCHASES_SUPPLIERS",
      "objective": "Manage purchase transactions from supplier sourcing/commitment to receipt/invoice-linked state with traceability.",
      "actors": ["Authorized purchasing operator", "Purchasing backend"],
      "preconditions": ["Authenticated tenant context", "Purchasing permission", "Supplier/items are valid"],
      "invariants": ["Purchase totals and lifecycle are backend-authoritative", "Supplier/document ownership is tenant-scoped", "Receipt/inventory/payable effects cannot be duplicated"],
      "happy_path": ["Create/open purchase", "Capture supplier/items/terms", "Backend validates and calculates", "Persist and expose authoritative state"],
      "alternate_flows": ["Invalid supplier/item/terms are rejected", "Forbidden transition fails closed", "Receipt/invoice discrepancies use explicit reconciliation paths"],
      "side_effects": ["Purchase document/lines may change", "Authorized receipt, inventory and payable flows may be enabled with traceability"],
      "idempotency": "Retries must not duplicate a purchase document or repeat the same receipt/financial effect."
    },
    {
      "matrix_id": "VAEP-MX::PURCHASES_SUPPLIERS::ORDENES_COMPRA",
      "domain": "PURCHASES_SUPPLIERS",
      "objective": "Create and manage purchase orders as approved commitments to suppliers before reception/invoicing.",
      "actors": ["Authorized purchasing operator", "Purchase-order backend"],
      "preconditions": ["Authenticated tenant context", "Purchase-order action is authorized", "Supplier/items are valid"],
      "invariants": ["Order status and totals are backend-authoritative", "Tenant ownership is enforced", "Reception/invoice linkage preserves document traceability"],
      "happy_path": ["Create/open order", "Select supplier/items and terms", "Backend validates/calculates", "Persist/transition order and return readback"],
      "alternate_flows": ["Invalid references/amounts are rejected", "Unauthorized or stale transitions fail", "Cancellation/closure cannot erase already traceable downstream activity"],
      "side_effects": ["Purchase order and lines/status may change", "Authorized downstream receptions/invoices may reference the order"],
      "idempotency": "Repeated create/transition requests do not duplicate the purchase order or the same lifecycle event."
    },
    {
      "matrix_id": "VAEP-MX::PURCHASES_SUPPLIERS::PROVEEDORES",
      "domain": "PURCHASES_SUPPLIERS",
      "objective": "Manage supplier master data used by purchasing, receiving and payable workflows.",
      "actors": ["Authorized purchasing administrator", "Supplier backend"],
      "preconditions": ["Authenticated tenant context", "Supplier action is authorized"],
      "invariants": ["Supplier identity is tenant-scoped", "Sensitive/payment/contact fields follow permission policy", "Referenced suppliers are not silently removed"],
      "happy_path": ["List/open supplier", "Create/edit permitted master data", "Backend validates keys/fields", "Persist and read back supplier"],
      "alternate_flows": ["Duplicate/invalid supplier data is rejected", "Forbidden access fails closed", "Protected dependent supplier follows explicit lifecycle policy"],
      "side_effects": ["Supplier master data may change", "Dependent purchasing documents may display updated supplier data"],
      "idempotency": "Retrying the same supplier state does not create duplicate suppliers."
    },
    {
      "matrix_id": "VAEP-MX::PURCHASES_SUPPLIERS::RECEPCIONES_COMPRA",
      "domain": "PURCHASES_SUPPLIERS",
      "objective": "Record purchase receptions against valid purchasing commitments and produce authoritative received-quantity/inventory effects.",
      "actors": ["Authorized receiving operator", "Receiving/inventory backend"],
      "preconditions": ["Authenticated tenant context", "Reception permission", "Referenced supplier/order/items are valid"],
      "invariants": ["Received quantities are server-validated", "A reception is traceable to its source and tenant", "Inventory effects occur once per accepted reception event"],
      "happy_path": ["Open source purchase/order", "Capture received quantities/metadata", "Backend validates remaining/allowed receipt", "Persist reception and resulting authoritative state"],
      "alternate_flows": ["Over-receipt/invalid references are rejected", "Duplicate/stale reception is prevented", "Forbidden action fails without inventory mutation"],
      "side_effects": ["Reception record/status may change", "Inventory and purchase fulfillment state may change transactionally"],
      "idempotency": "Retrying the same logical reception must not apply the same inventory increment twice."
    },
    {
      "matrix_id": "VAEP-MX::PURCHASES_SUPPLIERS::SOLICITUDES_COMPRA",
      "domain": "PURCHASES_SUPPLIERS",
      "objective": "Capture and manage internal purchase requests before supplier commitment, preserving approval/traceability boundaries.",
      "actors": ["Authorized requester/purchasing operator", "Purchase-request backend"],
      "preconditions": ["Authenticated tenant context", "Request action is permitted", "Requested items/context are valid"],
      "invariants": ["Request lifecycle is backend-authoritative", "Approval/ordering authority cannot be inferred from UI state", "Tenant ownership is enforced"],
      "happy_path": ["Create/open request", "Capture requested items/reason/context", "Backend validates", "Persist and expose authoritative lifecycle"],
      "alternate_flows": ["Invalid items/quantities are rejected", "Forbidden approval/transition fails", "Converted/closed request cannot be silently reconverted"],
      "side_effects": ["Purchase request and lines/status may change", "Authorized conversion may enable a traceable purchase order"],
      "idempotency": "Repeated save/transition requests must not duplicate the request or repeat the same conversion."
    },
    {
      "matrix_id": "VAEP-MX::INVENTORY_LOGISTICS::ALMACENES",
      "domain": "INVENTORY_LOGISTICS",
      "objective": "Manage warehouses as tenant-scoped inventory ownership/location anchors.",
      "actors": ["Authorized inventory administrator", "Inventory backend"],
      "preconditions": ["Authenticated tenant context", "Warehouse action is permitted"],
      "invariants": ["Warehouse identity is tenant-scoped", "Referenced warehouses cannot be silently deleted", "Inventory balances are not edited by warehouse master presentation state"],
      "happy_path": ["List/open warehouse", "Create/edit permitted master fields", "Backend validates constraints", "Persist and refresh"],
      "alternate_flows": ["Duplicate/invalid warehouse is rejected", "Forbidden mutation fails closed", "Warehouse with protected dependencies follows explicit lifecycle policy"],
      "side_effects": ["Warehouse master metadata may change", "Location/inventory workflows may consume updated warehouse state"],
      "idempotency": "Repeated desired warehouse state converges without duplicate warehouses."
    },
    {
      "matrix_id": "VAEP-MX::INVENTORY_LOGISTICS::INVENTARIO",
      "domain": "INVENTORY_LOGISTICS",
      "objective": "Present and control authoritative stock state and allowed inventory adjustments/movements across tenant locations.",
      "actors": ["Authorized inventory operator", "Inventory backend"],
      "preconditions": ["Authenticated tenant context", "Inventory action is permitted", "Product/warehouse/location references are valid"],
      "invariants": ["Stock quantities are derived from authoritative movements/transactions", "No client-only quantity override", "Movement effects are tenant-scoped and applied once"],
      "happy_path": ["Load authoritative balances/movements", "Initiate an allowed adjustment or movement", "Backend validates stock/rules", "Persist transactional movement and refresh balances"],
      "alternate_flows": ["Insufficient/invalid stock transition is rejected", "Stale/concurrent change returns controlled conflict", "Forbidden action creates no movement"],
      "side_effects": ["Inventory movement ledger and balances may change", "Related order/purchase fulfillment state may observe resulting stock"],
      "idempotency": "A retry of the same logical inventory movement must not apply quantity twice."
    },
    {
      "matrix_id": "VAEP-MX::INVENTORY_LOGISTICS::PREPARACIONES_PEDIDO_VENTA",
      "domain": "INVENTORY_LOGISTICS",
      "objective": "Prepare sales-order items for fulfillment using authoritative order and inventory state.",
      "actors": ["Authorized warehouse/fulfillment operator", "Fulfillment backend"],
      "preconditions": ["Authenticated tenant context", "Fulfillment action is permitted", "Referenced sales order is eligible"],
      "invariants": ["Prepared quantities cannot exceed authorized remaining quantities/stock", "Order and inventory tenant ownership must match", "Preparation effects are traceable and non-duplicated"],
      "happy_path": ["Open eligible order", "Capture picked/prepared quantities", "Backend validates order and stock", "Persist preparation and authoritative fulfillment state"],
      "alternate_flows": ["Insufficient stock/over-prepare is rejected", "Stale or already completed order is rejected", "Forbidden action changes nothing"],
      "side_effects": ["Preparation records/status may change", "Inventory reservation/movement and order fulfillment state may change through authoritative transaction"],
      "idempotency": "Retrying the same preparation event must not consume/prepare the same quantity twice."
    },
    {
      "matrix_id": "VAEP-MX::INVENTORY_LOGISTICS::SUCURSALES",
      "domain": "INVENTORY_LOGISTICS",
      "objective": "Manage tenant branches used to scope operational locations and business activity.",
      "actors": ["Authorized organization administrator", "Organization backend"],
      "preconditions": ["Authenticated tenant context", "Branch management permission"],
      "invariants": ["Branch identity is tenant-scoped", "Referenced branches cannot be silently removed", "Branch selection does not bypass authorization"],
      "happy_path": ["List/open branch", "Create/edit permitted branch data", "Backend validates constraints", "Persist and read back"],
      "alternate_flows": ["Duplicate/invalid branch is rejected", "Forbidden mutation fails closed", "Branch with dependencies follows explicit lifecycle policy"],
      "side_effects": ["Branch master data may change", "Operational location/report contexts may reflect the updated branch"],
      "idempotency": "Repeated application of the same branch state yields one branch record."
    },
    {
      "matrix_id": "VAEP-MX::INVENTORY_LOGISTICS::UBICACIONES_ALMACEN",
      "domain": "INVENTORY_LOGISTICS",
      "objective": "Manage warehouse sub-locations/bins used to locate inventory within a valid warehouse.",
      "actors": ["Authorized inventory administrator", "Inventory backend"],
      "preconditions": ["Authenticated tenant context", "Location management permission", "Parent warehouse is valid"],
      "invariants": ["Location belongs to exactly the valid tenant/warehouse context", "Referenced locations cannot be silently removed", "Stock state is changed through movement authority, not master-data edits"],
      "happy_path": ["List warehouse locations", "Create/edit a permitted location", "Backend validates parent and uniqueness", "Persist and refresh"],
      "alternate_flows": ["Invalid parent/duplicate location is rejected", "Forbidden action fails closed", "Location with stock/dependencies follows explicit lifecycle policy"],
      "side_effects": ["Warehouse location metadata may change", "Inventory placement options may change"],
      "idempotency": "Repeating the same location definition converges without duplicate locations."
    },
    {
      "matrix_id": "VAEP-MX::CASH_BANKS::CAJA",
      "domain": "CASH_BANKS",
      "objective": "Manage cash-register/session activity and cash movements with auditable balances and authorized lifecycle transitions.",
      "actors": ["Authorized cashier/finance operator", "Cash backend"],
      "preconditions": ["Authenticated tenant context", "Cash action is permitted", "Applicable register/session state is valid"],
      "invariants": ["Cash balance changes come from authoritative movements", "Open/close lifecycle is backend-authoritative", "A logical cash movement is applied once"],
      "happy_path": ["Open/load cash context", "Perform permitted open/movement/close action", "Backend validates state and amounts", "Persist movement/state and return authoritative balance"],
      "alternate_flows": ["Invalid amount/state is rejected", "Concurrent/stale lifecycle action returns controlled conflict", "Forbidden action produces no cash effect"],
      "side_effects": ["Cash session/movement/balance state may change", "Audit/accounting trace may be produced"],
      "idempotency": "Retries must not duplicate the same cash movement or lifecycle event."
    },
    {
      "matrix_id": "VAEP-MX::CASH_BANKS::CUENTAS_BANCARIAS",
      "domain": "CASH_BANKS",
      "objective": "Manage tenant bank-account master/configuration data used by financial transactions without exposing protected credentials.",
      "actors": ["Authorized finance administrator", "Finance backend"],
      "preconditions": ["Authenticated tenant context", "Bank-account management permission"],
      "invariants": ["Account identity is tenant-scoped", "Sensitive account/configuration data follows masking/permission rules", "Transactions are not fabricated through master-data edits"],
      "happy_path": ["List/open account", "Create/edit permitted account metadata", "Backend validates constraints", "Persist and read back sanitized state"],
      "alternate_flows": ["Duplicate/invalid account data is rejected", "Forbidden access fails closed", "Referenced account follows explicit lifecycle policy"],
      "side_effects": ["Bank-account master metadata may change", "Future financial transaction selection may change"],
      "idempotency": "Repeated desired account metadata converges without duplicate bank-account records."
    },
    {
      "matrix_id": "VAEP-MX::CASH_BANKS::METODOS_PAGO",
      "domain": "CASH_BANKS",
      "objective": "Manage supported payment methods and their eligibility/configuration for commercial and financial flows.",
      "actors": ["Authorized payment/finance administrator", "Payment configuration backend"],
      "preconditions": ["Authenticated tenant context", "Payment-method permission"],
      "invariants": ["Method eligibility is server-authoritative", "Sensitive provider/payment credentials are never stored as ordinary UI fields", "Referenced methods are not silently removed"],
      "happy_path": ["List methods", "Create/edit permitted method configuration", "Backend validates constraints", "Persist and expose sanitized authoritative state"],
      "alternate_flows": ["Invalid/duplicate method is rejected", "Forbidden mutation fails closed", "Provider/configuration failure does not expose secrets"],
      "side_effects": ["Payment-method availability/configuration may change", "Commercial/payment flows may consume updated eligibility"],
      "idempotency": "Repeating the same payment-method configuration yields one effective method state."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::ASIENTOS_CONTABLES",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Create and manage journal entries that preserve balanced, traceable accounting effects under authoritative period/account rules.",
      "actors": ["Authorized accounting operator", "Accounting backend"],
      "preconditions": ["Authenticated tenant context", "Accounting permission", "Accounts/period/context are valid"],
      "invariants": ["Debits and credits satisfy authoritative balance rules", "Posted entries preserve tenant/audit trace", "Closed-period or forbidden mutations fail closed"],
      "happy_path": ["Create/open entry", "Capture allowed lines/reference", "Backend validates accounts/period/balance", "Persist/post according to lifecycle and return readback"],
      "alternate_flows": ["Unbalanced/invalid entry is rejected", "Closed period or forbidden action is denied", "Reversal/correction uses explicit traceable policy"],
      "side_effects": ["Journal entry/lines/status may change", "Financial balances/reports may reflect posted entry"],
      "idempotency": "Retrying the same posting/reversal request must not create duplicate accounting effects."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::CENTROS_COSTO",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Manage cost-center classifications used to attribute financial activity and reporting.",
      "actors": ["Authorized accounting administrator", "Accounting backend"],
      "preconditions": ["Authenticated tenant context", "Cost-center management permission"],
      "invariants": ["Cost centers are tenant-scoped", "Referenced cost centers are not silently removed", "Financial postings rely on authoritative identity"],
      "happy_path": ["List/open cost center", "Create/edit permitted fields", "Backend validates constraints", "Persist and refresh"],
      "alternate_flows": ["Duplicate/invalid center is rejected", "Forbidden mutation fails closed", "Referenced center follows explicit lifecycle policy"],
      "side_effects": ["Cost-center metadata may change", "Future accounting/report classifications may change"],
      "idempotency": "Repeated desired cost-center state converges to one record."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::CUENTAS_POR_COBRAR",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Track customer receivables, balances, due state and authorized settlement/reconciliation effects.",
      "actors": ["Authorized receivables operator", "Receivables backend"],
      "preconditions": ["Authenticated tenant context", "Receivables permission", "Referenced customer/invoice/payment context is valid"],
      "invariants": ["Outstanding balance is derived from authoritative documents/payments", "Settlement cannot exceed/duplicate allowed effect", "Tenant/customer ownership is enforced"],
      "happy_path": ["Load receivable state", "Apply permitted settlement/reconciliation action", "Backend validates amounts/status", "Persist and return authoritative balance"],
      "alternate_flows": ["Overpayment/invalid reference is rejected", "Duplicate/stale settlement is prevented", "Forbidden action changes no balance"],
      "side_effects": ["Receivable/payment allocation/status may change", "Financial reports may reflect the authoritative balance"],
      "idempotency": "The same logical payment/allocation must not reduce the receivable more than once."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::ESTADOS_FINANCIEROS",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Produce financial statements from authoritative accounting data and period context without client-side recomputation authority.",
      "actors": ["Authorized finance/accounting reader", "Reporting/accounting backend"],
      "preconditions": ["Authenticated tenant context", "Financial-statement view permission", "Requested period/context is valid"],
      "invariants": ["Statement values derive from authoritative accounting records", "Tenant/period scope is enforced", "Presentation filters cannot alter source postings"],
      "happy_path": ["Select allowed statement/period", "Backend resolves authoritative data", "Statement is calculated/returned", "User can inspect/export only permitted result"],
      "alternate_flows": ["Invalid/closed/unavailable context returns controlled error/empty state", "Forbidden scope is denied", "Export preserves same authorized data scope"],
      "side_effects": ["Read/report generation only; optional export artifact may be produced without mutating accounting data"],
      "idempotency": "Repeated reads for the same underlying state and parameters are read-idempotent."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::FINANZAS",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Provide authorized finance operations/overview that orchestrate financial state without bypassing source accounting/cash/bank authorities.",
      "actors": ["Authorized finance operator", "Finance backend"],
      "preconditions": ["Authenticated tenant context", "Finance permission for requested operation"],
      "invariants": ["Displayed totals originate from authoritative sources", "Mutations delegate to explicit financial business paths", "Tenant scope is enforced"],
      "happy_path": ["Load finance context", "Inspect balances/operations", "Initiate only an authorized financial action", "Backend validates and returns authoritative result"],
      "alternate_flows": ["Unavailable source data yields controlled state", "Forbidden operation is denied", "Stale financial transition fails without partial effect"],
      "side_effects": ["Read operations have none", "Authorized delegated finance actions may create traceable financial effects"],
      "idempotency": "Reads are idempotent; delegated mutation retries inherit the target operation's non-duplication contract."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::IMPUESTOS",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Manage tax definitions/rates/eligibility used by authoritative commercial and accounting calculations.",
      "actors": ["Authorized accounting/tax administrator", "Tax backend"],
      "preconditions": ["Authenticated tenant context", "Tax-management permission"],
      "invariants": ["Applied tax is server-calculated/validated", "Tax definitions are tenant-scoped", "Historical document values are not silently rewritten by later configuration changes"],
      "happy_path": ["List taxes", "Create/edit permitted definition", "Backend validates rate/scope", "Persist and expose authoritative tax configuration"],
      "alternate_flows": ["Invalid/duplicate tax definition is rejected", "Forbidden change fails closed", "Protected historical dependencies follow explicit lifecycle policy"],
      "side_effects": ["Future transaction tax calculation may change", "Tax configuration audit state may change"],
      "idempotency": "Repeated same tax definition update converges to one effective configuration."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::PERIODOS_CONTABLES",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Manage accounting periods and their lifecycle so postings obey authoritative open/closed boundaries.",
      "actors": ["Authorized accounting administrator", "Accounting backend"],
      "preconditions": ["Authenticated tenant context", "Period-management permission"],
      "invariants": ["Period boundaries/lifecycle are backend-authoritative", "Closed periods reject unauthorized postings", "Overlapping/invalid period definitions are not accepted"],
      "happy_path": ["List/open period", "Create or transition permitted period state", "Backend validates chronology/dependencies", "Persist and return authoritative state"],
      "alternate_flows": ["Invalid overlap/date range is rejected", "Forbidden close/reopen is denied", "Period with blocking dependencies does not partially transition"],
      "side_effects": ["Accounting period metadata/status may change", "Posting eligibility may change"],
      "idempotency": "Repeating an already-applied valid period transition does not create a second lifecycle event/effect."
    },
    {
      "matrix_id": "VAEP-MX::FINANCE_ACCOUNTING::PLAN_CUENTAS",
      "domain": "FINANCE_ACCOUNTING",
      "objective": "Manage the chart of accounts used by accounting postings and reports with stable hierarchy and tenant ownership.",
      "actors": ["Authorized accounting administrator", "Accounting backend"],
      "preconditions": ["Authenticated tenant context", "Chart-of-accounts management permission"],
      "invariants": ["Account identity/hierarchy is tenant-scoped", "Invalid cycles/parents are rejected", "Accounts referenced by postings are not silently destroyed"],
      "happy_path": ["List/open account", "Create/edit permitted account metadata/hierarchy", "Backend validates constraints", "Persist and read back"],
      "alternate_flows": ["Duplicate/invalid code/hierarchy is rejected", "Forbidden change fails closed", "Referenced account follows explicit deactivate/reclassification policy"],
      "side_effects": ["Chart-of-accounts metadata/hierarchy may change", "Future postings/reporting classifications may use updated account state"],
      "idempotency": "Repeated same account definition converges without duplicate accounts."
    },
    {
      "matrix_id": "VAEP-MX::BI_REPORTING::CENTRO_REPORTES",
      "domain": "BI_REPORTING",
      "objective": "Provide a governed entry point to available reports while preserving each report's permission and data scope.",
      "actors": ["Authorized report consumer", "Reporting backend"],
      "preconditions": ["Authenticated tenant context", "At least one relevant report permission"],
      "invariants": ["Report discovery does not grant data access", "Each report enforces its own scope", "Tenant isolation applies to all report data"],
      "happy_path": ["Load permitted report catalog", "Choose a report", "Apply allowed parameters", "Backend returns authorized report result"],
      "alternate_flows": ["Forbidden report is hidden/denied", "Invalid parameters return controlled validation", "Unavailable data yields explicit empty/error state"],
      "side_effects": ["Read-only report navigation; exports may generate non-mutating artifacts"],
      "idempotency": "Report discovery/read operations are idempotent for the same source state and parameters."
    },
    {
      "matrix_id": "VAEP-MX::BI_REPORTING::DASHBOARD",
      "domain": "BI_REPORTING",
      "objective": "Present authorized operational/business summary metrics derived from authoritative tenant data.",
      "actors": ["Authenticated dashboard user", "Dashboard/reporting backend"],
      "preconditions": ["Authenticated tenant context", "Dashboard view permission"],
      "invariants": ["Metrics derive from authoritative sources", "Tenant scope is enforced", "Presentation aggregation cannot mutate source business data"],
      "happy_path": ["Load dashboard", "Backend resolves permitted metrics", "Cards/charts display current authoritative summary", "User navigates to permitted details"],
      "alternate_flows": ["Unavailable metrics show explicit empty/error state", "Forbidden detail is denied", "Partial source failure is not presented as fabricated complete data"],
      "side_effects": ["Read-only except client presentation state"],
      "idempotency": "Repeated dashboard reads are idempotent for the same source state."
    },
    {
      "matrix_id": "VAEP-MX::BI_REPORTING::RENTABILIDAD",
      "domain": "BI_REPORTING",
      "objective": "Report profitability from authoritative sales/cost/accounting sources under permitted filters.",
      "actors": ["Authorized finance/report consumer", "Reporting backend"],
      "preconditions": ["Authenticated tenant context", "Profitability report permission", "Requested filter scope is valid"],
      "invariants": ["Profitability values are calculated from authoritative sources", "Tenant/filter scope is enforced", "Report does not mutate source transactions"],
      "happy_path": ["Select period/filter", "Backend aggregates authorized source data", "Return profitability result", "User inspects/exports permitted view"],
      "alternate_flows": ["Invalid filters are rejected", "Missing data yields explicit empty/error state", "Forbidden scope is denied"],
      "side_effects": ["Read-only; optional export artifact"],
      "idempotency": "Same source state and parameters yield a repeatable read result."
    },
    {
      "matrix_id": "VAEP-MX::BI_REPORTING::REPORTES_ADMINISTRATIVOS",
      "domain": "BI_REPORTING",
      "objective": "Provide administrative reports over authorized tenant operational/configuration data.",
      "actors": ["Authorized administrative report consumer", "Reporting backend"],
      "preconditions": ["Authenticated tenant context", "Administrative report permission"],
      "invariants": ["Only authorized fields/entities are exposed", "Tenant isolation is enforced", "Reports do not grant mutation authority"],
      "happy_path": ["Choose report/filters", "Backend validates scope", "Return authorized dataset/aggregation", "User inspects/exports result"],
      "alternate_flows": ["Invalid filter returns validation", "Forbidden dataset is denied", "No data shows explicit empty state"],
      "side_effects": ["Read-only; optional export artifact"],
      "idempotency": "Report reads are idempotent for the same source state and parameters."
    },
    {
      "matrix_id": "VAEP-MX::BI_REPORTING::REPORTES_COMPRAS",
      "domain": "BI_REPORTING",
      "objective": "Report purchasing/supplier activity from authoritative purchase documents and related data.",
      "actors": ["Authorized purchasing/report consumer", "Reporting backend"],
      "preconditions": ["Authenticated tenant context", "Purchasing report permission"],
      "invariants": ["Values derive from authoritative purchasing sources", "Tenant and filter scope are enforced", "Reporting does not alter purchasing lifecycle"],
      "happy_path": ["Choose filters", "Backend aggregates authorized purchase data", "Return report", "User inspects/exports permitted result"],
      "alternate_flows": ["Invalid filters are rejected", "Forbidden scope is denied", "Unavailable/no data is explicit"],
      "side_effects": ["Read-only; optional export artifact"],
      "idempotency": "Repeated report reads are idempotent for unchanged source state."
    },
    {
      "matrix_id": "VAEP-MX::BI_REPORTING::REPORTES_INVENTARIO",
      "domain": "BI_REPORTING",
      "objective": "Report stock, movements and logistics state from authoritative inventory sources.",
      "actors": ["Authorized inventory/report consumer", "Reporting backend"],
      "preconditions": ["Authenticated tenant context", "Inventory report permission"],
      "invariants": ["Reported quantities derive from authoritative inventory state", "Tenant/warehouse scope is enforced", "Reporting cannot mutate stock"],
      "happy_path": ["Choose warehouse/product/date filters", "Backend validates scope and aggregates inventory data", "Return report", "User inspects/exports"],
      "alternate_flows": ["Invalid scope/filter is rejected", "Forbidden warehouse/tenant data is denied", "No data is explicit"],
      "side_effects": ["Read-only; optional export artifact"],
      "idempotency": "Repeated reads are idempotent for unchanged source state and filters."
    },
    {
      "matrix_id": "VAEP-MX::BI_REPORTING::REPORTES_VENTAS",
      "domain": "BI_REPORTING",
      "objective": "Report sales/commercial performance from authoritative sales/order/invoice sources.",
      "actors": ["Authorized sales/report consumer", "Reporting backend"],
      "preconditions": ["Authenticated tenant context", "Sales report permission"],
      "invariants": ["Metrics derive from authoritative commercial records", "Tenant/filter scope is enforced", "Report presentation cannot modify transactions"],
      "happy_path": ["Choose filters", "Backend validates and aggregates authorized sales data", "Return report", "User inspects/exports result"],
      "alternate_flows": ["Invalid filters are rejected", "Forbidden scope is denied", "No data is explicit"],
      "side_effects": ["Read-only; optional export artifact"],
      "idempotency": "Repeated reads are idempotent for unchanged source state and parameters."
    },
    {
      "matrix_id": "VAEP-MX::GOV_CONFIG_INTEGRATIONS::APP_SHELL",
      "domain": "GOV_CONFIG_INTEGRATIONS",
      "objective": "Provide the application shell, navigation and guard boundary that routes users only toward surfaces they may attempt to access.",
      "actors": ["Application user or storefront visitor", "Angular shell/guards", "Authoritative backend for protected actions"],
      "preconditions": ["Application bootstraps", "Route configuration is available"],
      "invariants": ["Client guards improve navigation but never replace backend authorization", "Protected internal routes require authenticated/authorized context", "Public routes expose only public contract data"],
      "happy_path": ["App resolves route", "Guard/navigation policy evaluates client context", "Allowed component loads", "Backend independently authorizes any protected data/action"],
      "alternate_flows": ["Unauthenticated protected route redirects/denies", "Unauthorized route denies", "Unknown route follows controlled not-found/fallback behavior"],
      "side_effects": ["Client navigation/session presentation state may change; shell itself must not create business mutations"],
      "idempotency": "Repeated navigation to the same allowed route is presentation-idempotent."
    },
    {
      "matrix_id": "VAEP-MX::GOV_CONFIG_INTEGRATIONS::AUDITORIA",
      "domain": "GOV_CONFIG_INTEGRATIONS",
      "objective": "Allow authorized users to inspect audit history without altering historical evidence through the viewing surface.",
      "actors": ["Authorized audit reader", "Audit backend"],
      "preconditions": ["Authenticated tenant context", "Auditoria view permission"],
      "invariants": ["Audit records are append-only/immutable from the viewer", "Sensitive fields are masked/redacted according to policy", "Tenant and permission scope are enforced"],
      "happy_path": ["Open audit view", "Apply permitted filters", "Backend returns authorized immutable events", "User inspects details"],
      "alternate_flows": ["Forbidden audit scope is denied", "Invalid filters return validation", "No matching events shows explicit empty state"],
      "side_effects": ["Read-only; viewing does not mutate audited business state"],
      "idempotency": "Repeated audit reads are idempotent for the same event set and filters."
    },
    {
      "matrix_id": "VAEP-MX::GOV_CONFIG_INTEGRATIONS::CARGAS_MASIVAS",
      "domain": "GOV_CONFIG_INTEGRATIONS",
      "objective": "Ingest supported bulk data through validated, authorized and traceable import workflows without bypassing domain invariants.",
      "actors": ["Authorized bulk-import operator", "Import backend", "Target domain services"],
      "preconditions": ["Authenticated tenant context", "Bulk-import permission", "Input file/type is supported"],
      "invariants": ["Every imported mutation is revalidated by authoritative domain rules", "Tenant scope cannot be supplied to escape current authority", "Invalid rows do not silently become accepted records"],
      "happy_path": ["Upload supported input", "Backend parses/validates", "Operator receives validation summary", "Accepted import applies through controlled transaction/batch semantics", "Result/evidence is returned"],
      "alternate_flows": ["Malformed/unsupported file is rejected", "Row-level validation failures are reported explicitly", "Forbidden import creates no target mutation", "Retry behavior prevents silent duplicate imports"],
      "side_effects": ["Accepted rows may create/update target domain records", "Import execution/audit/result state is recorded"],
      "idempotency": "A retried logical import must be identifiable and must not silently duplicate previously accepted rows."
    },
    {
      "matrix_id": "VAEP-MX::GOV_CONFIG_INTEGRATIONS::CONFIGURACION",
      "domain": "GOV_CONFIG_INTEGRATIONS",
      "objective": "Manage tenant/application configuration that is safe to expose and edit through authorized runtime settings.",
      "actors": ["Authorized configuration administrator", "Configuration backend"],
      "preconditions": ["Authenticated tenant context", "Configuration permission", "Requested key is an allowed runtime setting"],
      "invariants": ["Secrets are not exposed as ordinary configuration values", "Tenant-scoped settings cannot modify another tenant", "Invalid configuration is rejected before it can destabilize business rules"],
      "happy_path": ["Load allowed configuration", "Edit permitted value", "Backend validates type/range/policy", "Persist and read back sanitized effective configuration"],
      "alternate_flows": ["Unknown/protected key is denied", "Invalid value is rejected", "Concurrent change returns authoritative latest state/conflict"],
      "side_effects": ["Allowed runtime configuration may change", "Dependent features may consume new effective configuration"],
      "idempotency": "Applying the same configuration value repeatedly converges to one effective setting."
    }
  ]
}
