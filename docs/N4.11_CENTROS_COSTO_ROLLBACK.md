# N4.11.H.8 — Centros de costo: límites de rollback y verificación

Estado: `QA_TAKEOVER` para `N4.11.H.8.ROLLBACK_RUNBOOK` después de ATTEMPT2 sin entrega integrable por deriva de base. Este documento fija únicamente límites y verificaciones observables en `Desarrollo`; no ejecuta rollback, no autoriza deploy y no declara Producción recuperada.

## Alcance seguro

- Rama autorizada: `Desarrollo`.
- `main`, Producción, secretos, despliegues, force-push y reset destructivo permanecen fuera de alcance.
- El rollback de una entrega funcional debe hacerse mediante un nuevo cambio revisable en `Desarrollo`; no se reescribe la historia ni se reutiliza un manifest Jules.
- La restauración de base de datos solo puede probarse contra una base descartable de CI. No existe evidencia en este documento de una operación sobre datos productivos.

## Fronteras técnicas verificadas

- La migración `backend/src/Infrastructure/Migrations/20260908010500_N4_11_C_CentroCostoPersistencia.cs` contiene `Up` para `CentrosCosto` y un `Down` que elimina esa tabla. Por ser destructivo, el `Down` es una herramienta de recuperación de entornos descartables y requiere respaldo/verificación previa antes de cualquier uso autorizado.
- La transacción de aplicación expone rollback de la unidad de trabajo en `backend/src/Infrastructure/Services/UnitOfWork.cs`; no sustituye la reversión de una migración ni garantiza recuperación de datos por sí sola.
- La configuración vigente de `CentroCosto` está en `backend/src/Infrastructure/Persistence/Configurations/CentroCostoConfiguration.cs`; cualquier rollback de esquema debe conservar la correspondencia entre configuración, migración y snapshot.

## Procedimiento de verificación en Desarrollo/CI

1. Identificar el commit funcional y la migración/artefacto que se desea revertir; conservar su SHA y el motivo causal.
2. Ejecutar build y pruebas causales del checkout candidato antes de publicar un cambio de reversión.
3. Para persistencia, usar exclusivamente el workflow de recovery con MySQL descartable y comprobar migración, arranque limpio, segunda ejecución y rollback en el artefacto de CI.
4. Verificar que no existan cambios a `main`, Producción ni datos productivos y registrar el resultado en un receipt VAEP.
5. Repetir las pruebas de aceptación de Centros de costo después de la reversión y dejar el cambio sujeto a `REVIEW_FIRST`.

Evidencia de gates aplicables al functional head: `desarrollo-ci.yml` run `34242471748` y `catalogos-aceptacion.yml` run `34242471747`, ambos `SUCCESS` sobre `b30b949e258e0b5e0476dcbe91419c006b739d39`. La evidencia no implica que se haya ejecutado un rollback.

## Limitaciones y decisión QA

- No se encontró un procedimiento seguro para revertir datos productivos, ni se inventa uno.
- No se ejecutó `Down`, restore, deploy ni operación remota.
- La entrega Jules ATTEMPT2 no se integra porque su `baseCommitId` no coincide con `primaryBaseHead`; el gap documental se resuelve por este takeover factual y no habilita R3.
- El cierre de `N4.11.H` requiere el receipt agregado de REVIEW_FIRST, gates causales terminales y `P0=0/P1=0` conforme al MAESTRO.
