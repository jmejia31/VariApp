# N5.1 — Reportes: Decisión de Dominio y Contratos (N5.1.B)

## Decisión: N/A (no aplica nuevo contrato de dominio)

REVIEW_FIRST de CHATGPT_BUSINESS sobre la entrega Jules `N5.1.B.1.DOMAIN_CONTRACT_DECISION`, sesión `sessions/1256753713078391601`, workflow `34287875184`.

No se requiere crear un nuevo contrato de dominio o una fachada unificada como `IReporteCentralService` para el Centro de Reportes en esta etapa. La decisión conserva los contratos especializados existentes y evita duplicar lógica solo para ofrecer una entrada de navegación unificada.

## Evidencia exacta

- `backend/src/Application/Interfaces/IReporteAdministrativoService.cs`: `IReporteAdministrativoService` ya expone resumen administrativo, usuarios/accesos, roles/permisos, auditoría y exportación.
- `backend/src/Application/Interfaces/IEstadoFinancieroService.cs`: `IEstadoFinancieroService` ya expone generación de estados financieros.
- `docs/N5.1_REPORTES_PREFLIGHT_ARCHITECTURE.md`: el preflight aceptado exige reusar contratos especializados, mantener ownership de dominio y no introducir una facade sin un caso de uso cross-domain concreto.
- `frontend/src/app/features/reportes-administrativos/reportes-administrativos.component.ts`: existe consumidor UI independiente para reportes administrativos.
- El preflight aceptado identifica `frontend/src/app/features/estados-financieros/estados-financieros.component.ts` como consumidor UI de estados financieros.

## Invariantes de ownership

1. La lógica financiera permanece bajo `EstadoFinancieroService` / `IEstadoFinancieroService`.
2. La lógica administrativa, auditoría y reportes administrativos permanece bajo `ReporteAdministrativoService` / `IReporteAdministrativoService`.
3. El Centro de Reportes no duplica algoritmos de negocio para unificar navegación.
4. No se crea persistencia nueva únicamente para presentar un shell central.
5. No se inventan permisos o grants nuevos sin una decisión explícita posterior y evidencia de necesidad.

## Restricciones para los siguientes parents

- **N5.1.C — Persistencia, migración y datos:** partir de esta decisión para demostrar N/A cuando la composición use contratos de lectura existentes. Cualquier necesidad real de persistencia nueva debe justificarse como gap material separado.
- **N5.1.D — Aplicación, servicios y API:** reutilizar endpoints/servicios existentes mientras no exista una operación coherente que necesite composición cross-domain. No crear controlador/facade agregador por conveniencia.
- **N5.1.E — Frontend y UX:** el Centro de Reportes puede actuar como ruta/shell independiente que compone o enlaza capacidades existentes, preservando loading/error/accessibility y autorización fail-closed.
- **N5.1.F — Seguridad:** mantener el contrato RBAC existente y no ampliar privilegios.

## Validación de la entrega Jules

- Patch material: sí; único archivo de scope aceptado: este documento.
- Base: equivalente por descendencia de control-plane según el terminal contract de la entrega.
- `SELF_REVIEW_PASS_1`: reportado por Jules.
- `SELF_REVIEW_PASS_2`: reportado por Jules.
- `TESTS_EXECUTED`: Jules reportó frontend 94 PASS y backend 1804 PASS / 24 fallos asociados a conexión MySQL; esos fallos no se convierten aquí en PASS ni se atribuyen a este cambio documental.
- Inspección independiente de CHATGPT_BUSINESS: los dos contratos citados existen en el HEAD revisado y la decisión coincide con el preflight arquitectónico ya aceptado.
- Producción, `main`, secretos, deploys y migraciones: no tocados por esta integración documental.

## Resultado REVIEW_FIRST

`PASS` para el alcance documental `N5.1.B.1.DOMAIN_CONTRACT_DECISION`.

Esta decisión no certifica por sí sola `N5.1.B` como `LISTO_REAL`; el cierre del parent sigue sujeto al MAESTRO, recibo de revisión, DoD y gates/CI aplicables.
