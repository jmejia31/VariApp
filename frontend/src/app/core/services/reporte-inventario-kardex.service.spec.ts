import { HttpClientTestingModule, HttpTestingController } from '@angular/common/http/testing';import { TestBed } from '@angular/core/testing';import { environment } from '../../../environments/environment';import { ReporteInventarioKardexService } from './reporte-inventario-kardex.service';describe('ReporteInventarioKardexService',()=>{let service:ReporteInventarioKardexService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({imports:[HttpClientTestingModule]});service=TestBed.inject(ReporteInventarioKardexService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('usa la ruta y envelope server-side canónicos',()=>{service.get({page:2,pageSize:25,sortBy:'Fecha',sortDirection:'desc',productoId:7,almacenId:3}).subscribe(res=>expect(res.data.page).toBe(2));const req=http.expectOne(r=>r.url===`${environment.apiUrl}/inventario/reportes/kardex`&&r.params.get('page')==='2'&&r.params.get('productoId')==='7');expect(req.request.method).toBe('GET');req.flush({success:true,message:'ok',errors:[],data:{items:[],page:2,pageSize:25,totalCount:0,totalPages:0}});});});
