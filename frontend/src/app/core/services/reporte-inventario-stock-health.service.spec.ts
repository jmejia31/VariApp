import {HttpClientTestingModule,HttpTestingController} from '@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{environment}from'../../../environments/environment';import{ReporteInventarioStockHealthService}from'./reporte-inventario-stock-health.service';describe('ReporteInventarioStockHealthService',()=>{let service:ReporteInventarioStockHealthService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({imports:[HttpClientTestingModule]});service=TestBed.inject(ReporteInventarioStockHealthService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('envía tipo y ventana acotada al endpoint canónico',()=>{service.get({page:1,pageSize:20,sortBy:'Fecha',sortDirection:'desc',tipoReporte:5,dias:30}).subscribe(r=>expect(r.success).toBe(true));const req=http.expectOne(r=>r.url===`${environment.apiUrl}/inventario/reportes/stock-health`&&r.params.get('tipoReporte')==='5'&&r.params.get('dias')==='30');req.flush({success:true,message:'ok',errors:[],data:{items:[],page:1,pageSize:20,totalCount:0,totalPages:0}});});});
