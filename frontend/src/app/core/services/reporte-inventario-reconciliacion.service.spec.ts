import{HttpClientTestingModule,HttpTestingController}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{environment}from'../../../environments/environment';import{ReporteInventarioReconciliacionService}from'./reporte-inventario-reconciliacion.service';describe('ReporteInventarioReconciliacionService',()=>{let service:ReporteInventarioReconciliacionService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({imports:[HttpClientTestingModule]});service=TestBed.inject(ReporteInventarioReconciliacionService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('mantiene paging y origen en el servidor',()=>{service.get({page:1,pageSize:20,sortBy:'Fecha',sortDirection:'desc',origen:2}).subscribe(r=>expect(r.data.items.length).toBe(0));const req=http.expectOne(r=>r.url===`${environment.apiUrl}/inventario/reportes/reconciliacion`&&r.params.get('origen')==='2');req.flush({success:true,message:'ok',errors:[],data:{items:[],page:1,pageSize:20,totalCount:0,totalPages:0}});});});
