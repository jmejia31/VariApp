import { CommonModule } from '@angular/common';
import { Component, EventEmitter, Output, inject } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { ReporteInventarioFiltroBaseDto } from '../../../../core/models/reporte-inventario.models';
@Component({selector:'app-reporte-inventario-filtro-base',standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<form class="filter-grid" [formGroup]="form" (ngSubmit)="emitir()" aria-label="Filtros comunes del reporte"><label>Desde<input type="date" formControlName="desde"></label><label>Hasta<input type="date" formControlName="hasta"></label><label>Sucursal<input type="number" min="1" formControlName="sucursalId"></label><label>Almacén<input type="number" min="1" formControlName="almacenId"></label><label>Ubicación<input type="number" min="1" formControlName="ubicacionAlmacenId"></label><label>Tamaño de página<select formControlName="pageSize"><option [ngValue]="20">20</option><option [ngValue]="50">50</option><option [ngValue]="100">100</option></select></label><button type="submit" [disabled]="form.invalid || rangoInvalido">Aplicar filtros</button>@if (rangoInvalido) { <p class="error" role="alert">Desde no puede ser posterior a Hasta.</p> }</form>`,styles:[`.filter-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:.75rem;align-items:end}.filter-grid label{display:grid;gap:.25rem}.filter-grid input,.filter-grid select,.filter-grid button{min-height:2.5rem}.error{grid-column:1/-1;color:#b91c1c;margin:0}`]})
export class ReporteInventarioFiltroBaseComponent{private readonly fb=inject(FormBuilder);@Output()readonly filterChanged=new EventEmitter<ReporteInventarioFiltroBaseDto>();readonly form=this.fb.group({desde:[''],hasta:[''],sucursalId:[null as number|null,Validators.min(1)],almacenId:[null as number|null,Validators.min(1)],ubicacionAlmacenId:[null as number|null,Validators.min(1)],pageSize:[20,[Validators.required,Validators.min(1),Validators.max(100)]]});get rangoInvalido():boolean{const{desde,hasta}=this.form.getRawValue();return!!desde&&!!hasta&&desde>hasta;}emitir():void{if(this.form.invalid||this.rangoInvalido)return;const v=this.form.getRawValue();this.filterChanged.emit({page:1,pageSize:v.pageSize??20,sortBy:'Fecha',sortDirection:'desc',...(v.desde?{desde:v.desde}:{}),...(v.hasta?{hasta:v.hasta}:{}),...(v.sucursalId?{sucursalId:v.sucursalId}:{}),...(v.almacenId?{almacenId:v.almacenId}:{}),...(v.ubicacionAlmacenId?{ubicacionAlmacenId:v.ubicacionAlmacenId}:{})});}}
