import { CommonModule } from '@angular/common';import { Component,OnInit,inject } from '@angular/core';import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms';import { MatButtonModule } from '@angular/material/button';import { MatProgressSpinnerModule } from '@angular/material/progress-spinner';import { MovimientoInventarioReporteDto,ReporteInventarioKardexService } from '../../../core/services/reporte-inventario-kardex.service';
@Component({selector:'app-reporte-inventario-kardex',standalone:true,imports:[CommonModule,ReactiveFormsModule,MatButtonModule,MatProgressSpinnerModule],template:`<section class="report-page" aria-labelledby="kardex-title"><header><h2 id="kardex-title">Kardex de inventario</h2><p>Movimientos paginados por el servidor; la UI no reconstruye semántica de inventario.</p></header><form class="filters" [formGroup]="form" (ngSubmit)="buscar(true)"><label>Producto ID<input type="number" min="1" formControlName="productoId"></label><label>Variante ID<input type="number" min="1" formControlName="productoVarianteId"></label><label>Almacén ID<input type="number" min="1" formControlName="almacenId"></label><label>Ubicación ID<input type="number" min="1" formControlName="ubicacionAlmacenId"></label><label>Desde<input type="date" formControlName="desde"></label><label>Hasta<input type="date" formControlName="hasta"></label><label>Tipo<input type="text" maxlength="50" formControlName="tipo"></label><label>Causa<input type="text" maxlength="80" formControlName="causa"></label><button mat-flat-button type="submit" [disabled]="loading||form.invalid||rangoInvalido">Consultar</button></form>@if(rangoInvalido){<p role="alert" class="error">Desde no puede ser posterior a Hasta.</p>}@if(loading){<div class="state"><mat-spinner diameter="30"></mat-spinner>Consultando Kardex…</div>}@else if(error){<div class="state error" role="alert">{{error}}</div>}@else if(rows.length===0){<div class="state" role="status">No hay movimientos para los filtros actuales.</div>}@else{<div class="table-wrap"><table><thead><tr><th>Fecha</th><th>Producto</th><th>SKU</th><th>Tipo</th><th>Causa</th><th>Cantidad</th><th>Stock</th><th>Origen</th><th>Correlación</th></tr></thead><tbody>@for(row of rows;track row.id){<tr><td>{{row.fecha|date:'short'}}</td><td>{{row.productoNombre}}</td><td>{{row.productoSku||'—'}}</td><td>{{row.tipo}}</td><td>{{row.causa}}</td><td>{{row.cantidad}}</td><td>{{row.stockAnterior}} → {{row.stockNuevo}}</td><td>{{row.origenTipo||row.referenciaTipo}} {{row.origenId||row.referenciaId}}</td><td><code>{{row.correlationId}}</code></td></tr>}</tbody></table></div><div class="pager"><button mat-stroked-button type="button" (click)="pagina(-1)" [disabled]="page<=1">Anterior</button><span>Página {{page}} de {{totalPages||1}}</span><button mat-stroked-button type="button" (click)="pagina(1)" [disabled]="page>=totalPages">Siguiente</button></div>}</section>`,styles:[`.report-page{display:grid;gap:1rem}.report-page header h2,.report-page header p{margin:0}.filters{display:grid;grid-template-columns:repeat(auto-fit,minmax(145px,1fr));gap:.65rem;align-items:end}.filters label{display:grid;gap:.25rem}.filters input,.filters button{min-height:2.5rem}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse}th,td{padding:.55rem;border-bottom:1px solid var(--border-color,#ddd);text-align:left;white-space:nowrap}.state{padding:1rem;display:flex;gap:.75rem;align-items:center}.error{color:#b91c1c}.pager{display:flex;gap:.75rem;align-items:center;justify-content:flex-end}`]})
export class KardexComponent implements OnInit{private readonly fb=inject(FormBuilder);private readonly service=inject(ReporteInventarioKardexService);readonly form=this.fb.group({productoId:[null as number|null,Validators.min(1)],productoVarianteId:[null as number|null,Validators.min(1)],almacenId:[null as number|null,Validators.min(1)],ubicacionAlmacenId:[null as number|null,Validators.min(1)],desde:[''],hasta:[''],tipo:[''],causa:['']});rows:MovimientoInventarioReporteDto[]=[];loading=false;error='';page=1;pageSize=20;totalPages=0;ngOnInit():void{this.buscar(true);}get rangoInvalido():boolean{const v=this.form.getRawValue();return!!v.desde&&!!v.hasta&&v.desde>v.hasta;}buscar(reset=false):void{if(this.form.invalid||this.rangoInvalido)return;if(reset)this.page=1;this.loading=true;this.error='';const v=this.form.getRawValue();this.service.get({page:this.page,pageSize:this.pageSize,sortBy:'Fecha',sortDirection:'desc',...(v.productoId?{productoId:v.productoId}:{}),...(v.productoVarianteId?{productoVarianteId:v.productoVarianteId}:{}),...(v.almacenId?{almacenId:v.almacenId}:{}),...(v.ubicacionAlmacenId?{ubicacionAlmacenId:v.ubicacionAlmacenId}:{}),...(v.desde?{desde:v.desde}:{}),...(v.hasta?{hasta:v.hasta}:{}),...(v.tipo?{tipo:v.tipo.trim()}:{}),...(v.causa?{causa:v.causa.trim()}:{})}).subscribe({next:r=>{this.rows=r.data.items;this.totalPages=r.data.totalPages;this.loading=false;},error:()=>{this.error='No se pudo consultar el Kardex.';this.loading=false;}});}pagina(delta:number):void{const n=this.page+delta;if(n<1||(this.totalPages&&n>this.totalPages))return;this.page=n;this.buscar();}}
