import{CommonModule}from'@angular/common';import{Component,OnInit,inject}from'@angular/core';import{FormBuilder,ReactiveFormsModule,Validators}from'@angular/forms';import{MatButtonModule}from'@angular/material/button';import{MatProgressSpinnerModule}from'@angular/material/progress-spinner';import{PermisosRuntimeService}from'../../../core/auth/permisos-runtime.service';import{OrigenReconciliacionInventario,ReporteInventarioReconciliacionService,ReporteReconciliacionDto}from'../../../core/services/reporte-inventario-reconciliacion.service';
@Component({selector:'app-reporte-reconciliacion',standalone:true,imports:[CommonModule,ReactiveFormsModule,MatButtonModule,MatProgressSpinnerModule],template:`<section class="report-page" aria-labelledby="recon-title"><header><h2 id="recon-title">Reconciliación de inventario</h2><p>Conteos, transferencias y ajustes con trazabilidad server-side. Los campos financieros se muestran sólo cuando el API y permisos los habilitan.</p></header><form class="filters" [formGroup]="form" (ngSubmit)="buscar(true)"><label>Origen<select formControlName="origen"><option [ngValue]="null">Todos</option><option [ngValue]="1">Conteo</option><option [ngValue]="2">Transferencia</option><option [ngValue]="3">Ajuste</option></select></label><label>Producto ID<input type="number" min="1" formControlName="productoId"></label><label>Variante ID<input type="number" min="1" formControlName="productoVarianteId"></label><label>SKU<input type="text" maxlength="100" formControlName="sku"></label><label>Almacén ID<input type="number" min="1" formControlName="almacenId"></label><label>Desde<input type="date" formControlName="desde"></label><label>Hasta<input type="date" formControlName="hasta"></label><button mat-flat-button type="submit" [disabled]="loading||form.invalid||rangoInvalido">Consultar</button></form>@if(rangoInvalido){<p class="error" role="alert">Desde no puede ser posterior a Hasta.</p>}@if(loading){<div class="state"><mat-spinner diameter="30"></mat-spinner>Consultando reconciliación…</div>}@else if(error){<div class="state error" role="alert">{{error}}</div>}@else if(rows.length===0){<div class="state" role="status">Sin resultados.</div>}@else{<div class="table-wrap"><table><thead><tr><th>Fecha</th><th>Origen</th><th>Documento</th><th>Producto</th><th>SKU</th><th>Esperada</th><th>Observada</th><th>Diferencia</th><th>Usuario</th><th>Correlación</th>@if(puedeVerFinanzas()){<th>Costo unit.</th><th>Impacto</th>}</tr></thead><tbody>@for(row of rows;track row.origen+'-'+row.documentoId+'-'+row.productoId+'-'+row.productoVarianteId){<tr><td>{{row.fecha|date:'short'}}</td><td>{{origenLabel(row.origen)}}</td><td>{{row.numeroDocumento}}</td><td>{{row.productoNombre}}</td><td>{{row.sku||'—'}}</td><td>{{row.cantidadEsperada}}</td><td>{{row.cantidadObservada}}</td><td>{{row.diferenciaNormalizada}}</td><td>{{row.usuarioNombre||'—'}}</td><td><code>{{row.correlationId||'—'}}</code></td>@if(puedeVerFinanzas()){<td>{{row.costoUnitario===null?'—':(row.costoUnitario|currency)}}</td><td>{{row.impactoCosto===null?'—':(row.impactoCosto|currency)}}</td>}</tr>}</tbody></table></div><div class="pager"><button mat-stroked-button type="button" (click)="pagina(-1)" [disabled]="page<=1">Anterior</button><span>Página {{page}} de {{totalPages||1}}</span><button mat-stroked-button type="button" (click)="pagina(1)" [disabled]="page>=totalPages">Siguiente</button></div>}</section>`,styles:[`.report-page{display:grid;gap:1rem}.report-page header h2,.report-page header p{margin:0}.filters{display:grid;grid-template-columns:repeat(auto-fit,minmax(145px,1fr));gap:.65rem;align-items:end}.filters label{display:grid;gap:.25rem}.filters input,.filters select,.filters button{min-height:2.5rem}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse}th,td{padding:.55rem;border-bottom:1px solid var(--border-color,#ddd);text-align:left;white-space:nowrap}.state{padding:1rem;display:flex;gap:.75rem;align-items:center}.error{color:#b91c1c}.pager{display:flex;gap:.75rem;justify-content:flex-end;align-items:center}`]})
export class ReconciliacionComponent implements OnInit{private readonly fb=inject(FormBuilder);private readonly service=inject(ReporteInventarioReconciliacionService);private readonly permisos=inject(PermisosRuntimeService);readonly form=this.fb.group({origen:[null as OrigenReconciliacionInventario|null],productoId:[null as number|null,Validators.min(1)],productoVarianteId:[null as number|null,Validators.min(1)],sku:[''],almacenId:[null as number|null,Validators.min(1)],desde:[''],hasta:['']});rows:ReporteReconciliacionDto[]=[];loading=false;error='';page=1;pageSize=20;totalPages=0;ngOnInit():void{this.buscar(true);}puedeVerFinanzas():boolean{return this.permisos.puede('Finanzas','Ver');}get rangoInvalido():boolean{const v=this.form.getRawValue();return!!v.desde&&!!v.hasta&&v.desde>v.hasta;}origenLabel(v:OrigenReconciliacionInventario):string{return v===1?'Conteo':v===2?'Transferencia':'Ajuste';}buscar(reset=false):void{if(this.form.invalid||this.rangoInvalido)return;if(reset)this.page=1;this.loading=true;this.error='';const v=this.form.getRawValue();this.service.get({page:this.page,pageSize:this.pageSize,sortBy:'Fecha',sortDirection:'desc',...(v.origen?{origen:v.origen}:{}),...(v.productoId?{productoId:v.productoId}:{}),...(v.productoVarianteId?{productoVarianteId:v.productoVarianteId}:{}),...(v.sku?{sku:v.sku.trim()}:{}),...(v.almacenId?{almacenId:v.almacenId}:{}),...(v.desde?{desde:v.desde}:{}),...(v.hasta?{hasta:v.hasta}:{})}).subscribe({next:r=>{this.rows=r.data.items;this.totalPages=r.data.totalPages;this.loading=false;},error:()=>{this.error='No se pudo consultar la reconciliación.';this.loading=false;}});}pagina(d:number):void{const n=this.page+d;if(n<1||(this.totalPages&&n>this.totalPages))return;this.page=n;this.buscar();}}
