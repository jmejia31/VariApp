{
  "dispatchId": "VAEP-JULES-A-N28E-FRONTEND-A1-V321-20260822T1959Z",
  "taskId": "N2.8.E.A1",
  "workerId": "JULES_A",
  "expectedBranch": "Desarrollo",
  "primaryBaseHead": "740c40c9a50af9dc75550cc478dec717e1f5b7a1",
  "taskAttempt": 1,
  "fileScopeHint": "WRITE_SCOPE: frontend/src/app/features/finanzas/cuentas-por-pagar/**, frontend/src/app/core/models/**cuenta-por-pagar**, frontend/src/app/core/services/**cuentas-por-pagar**, and ONLY the minimum route/navigation wiring physically required for N2.8.E; NO backend; NO package-lock; NO workflows; NO manifests; NO unrelated finance screens",
  "prompt": "CURRENT_PARENT=N2.8.E Frontend y UX. PARENT_CLOSE_FIRST. Implement the authoritative frontend slice for Cuentas por pagar against PRIMARY_BASE_HEAD and the certified N2.8.D API. First inspect the real controller/DTOs and existing Angular conventions. Backend contract is authoritative: route cuentas-por-pagar; GET list with CuentaPorPagarFiltroDto; GET {id}; POST generar with FacturaProveedorId + CondicionPago + optional FechaVencimientoUtc; POST {id}/aplicaciones; POST {id}/aplicaciones/revertir; POST {id}/anular. Permissions are ModuloSistema.Finanzas with Ver/Crear/Editar/Anular. Build a production-quality standalone feature under finanzas/cuentas-por-pagar with typed model/service, list/detail UX, filters, loading/empty/error states, responsive accessible markup, clear saldo/estado/moneda/vencimiento, and only actions supported by the real API. Use existing ApiResponse/PagedResult and auth/permission patterns from the repo; do not invent FX, DELETE, undocumented transitions, endpoints, fields or permissions. Sanitize backend errors; confirmations must be explicit for destructive/reversal operations. Wire only the minimum route/navigation physically needed and avoid unrelated finance changes. Run npm ci only if needed without modifying lockfile, lint, production build and focused tests available. Before COMPLETED perform two separate final reviews and emit two distinct activity messages literally containing SELF_REVIEW_PASS_1=PASS and SELF_REVIEW_PASS_2=PASS. Report exact baseCommitId, full changed-file list, tests, limitations, observations, risks and recommendations. Do not push/merge/create branch/PR/deploy. Return reviewable patch only."
}
