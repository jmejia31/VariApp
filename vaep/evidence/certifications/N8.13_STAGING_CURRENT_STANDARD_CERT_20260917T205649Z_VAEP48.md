# N8.13 — Staging comparable a producción — Certificación current-standard

Fecha UTC: 2026-09-17T20:56:49Z  
Rama: `Desarrollo`  
Rol: `DEBT_BUILDER_CLOSER`  
Autoridad operativa: `docs/VAEP_AUTHORITY.md`

## 1. Alcance certificado

Esta certificación cubre `N8.13` y, específicamente para `N8.13.H (DOC_CERT)`, consolida la evidencia current-standard de que el entorno de Desarrollo usado como staging mantiene una configuración y una ruta de ejecución comparables con producción sin realizar writes sobre producción.

La revalidación histórica no se usa como sustituto del estándar actual. Los cierres anteriores son apoyo; los puntos `N8.13.F` y `N8.13.G` fueron revalidados de forma fresca en esta misma cadena y constituyen la base inmediata de esta certificación.

## 2. Evidencia current-standard inmediata

- `N8.13.F / SEC_AUDIT`: REVIEW_FIRST fresco PASS y receipt fresco LISTO.
- `N8.13.G / TEST_CI`: REVIEW_FIRST fresco PASS y receipt fresco LISTO.
- Gate causal de seguridad: workflow run `35258962286` — `success`.
- Gate causal de calidad/regresión: workflow run `35258969104` — `success`.
- Head probado por ambos gates: `6fd3e28cbf28164d110d6b83756b9094cec654a6`.
- Equivalencia material demostrada al cierre documental: backend tree `f8db476310130c169ddb5d8c4ea03cfbab9617f6`; frontend tree `3d7847eb8817b14942746f1e31114e56c15efbdd`.
- Los commits posteriores al head probado son de evidencia/control y no alteran esos árboles de producto.

## 3. Runtime de Desarrollo verificado

Verificación read-only fresca realizada durante `N8.13.G`:

- Proyecto Vercel de Desarrollo: `variapp-desarrollo`.
- Alias canónico: `variapp-desarrollo.vercel.app`.
- Deployment observado: `dpl_3epaGmgBtXmvEhjW8iBU46UFy12a`.
- Estado: `READY`.
- Ref: `Desarrollo`.
- Root HTTP: `200`.
- `/api/health/ready`: HTTP `200` con `status=ready` y `database=connected`.
- Headers de seguridad esperados presentes.
- Target backend de Desarrollo: `variapp-api-desarrollo.onrender.com`.

## 4. Comparabilidad con producción sin write productivo

La inspección de producción fue estrictamente read-only:

- Proyecto activo observado: `varistorehn`.
- Alias: `varistorehn.vercel.app`.
- Deployment observado: `dpl_53hXtv5e8oedhKe6wQRCtz5bDHfq`.
- Ref: `main`.
- SHA observado: `85b4e02814823e9671803c23798a6ff0bf05c8f6`.
- Estado: `READY`.
- No se realizó deploy, cambio de dominio, certificado, secreto, variable, DNS ni write de producción.

La semántica de routing quedó reconciliada:

- Build de Desarrollo usa `apiBaseUrl=/api`.
- `frontend/vercel.json` enruta el fallback `/api` de Desarrollo a `https://variapp-api-desarrollo.onrender.com`.
- La regla por host de producción enruta a `https://variapp-api.onrender.com`.
- El bundle activo de `main` usa directamente `https://variapp-api.onrender.com`.
- Por ello, la respuesta SPA al path same-origin de producción no implica un fallo de API del runtime activo; el bundle productivo no depende de ese same-origin `/api`.

Conclusión: staging y producción están aislados por entorno, pero mantienen equivalencia de intención y target semántico para la ruta API que corresponde a cada entorno.

## 5. Seguridad, tenant y persistencia

La revalidación current-standard mantiene:

- autenticación, RBAC y aislamiento tenant sin regresión material detectada;
- CORS/AllowedHosts limitados por entorno;
- no lectura ni exposición de valores de secretos;
- no cambios de persistencia, esquema o migraciones en este cierre documental;
- no cambio de API/OpenAPI, ADR ni ERD requerido por ausencia de delta funcional;
- `P0=0`, `P1=0`, `P2=0` al cierre de TEST_CI y en REVIEW_FIRST de DOC_CERT.

## 6. Runbook de revalidación segura de staging

1. Releer `docs/VAEP_AUTHORITY.md` y confirmar `Desarrollo` como única rama escribible para VAEP.
2. Releer HEAD, `CONFIG`, `COLA`, `CONTROL_TOWER`, lease y evidencia; respetar single-writer.
3. Verificar que el árbol de producto del head candidato sea el mismo que el head probado o demostrar equivalencia exacta de backend/frontend.
4. Confirmar que los gates causales de seguridad y calidad/regresión sean terminales y exitosos para ese producto equivalente.
5. Inspeccionar read-only el deployment de `variapp-desarrollo`, su estado y ref.
6. Ejecutar GET read-only sobre la raíz de staging y `/api/health/ready`; exigir `200`, `ready` y conexión de base de datos.
7. Verificar headers de seguridad y revisar `frontend/vercel.json` más los archivos de environment del head de Desarrollo.
8. Si se necesita comparar producción, hacerlo únicamente con metadata/GET read-only. No cambiar deploys, variables, secretos, dominios, certificados ni DNS.
9. Ante cualquier P0/P1, reabrir únicamente el scope causal, corregir en Desarrollo, repetir pruebas/gates aplicables y recertificar.
10. Persistir REVIEW_FIRST, receipt y write/readback final antes de declarar `LISTO`.

## 7. Rollback de Desarrollo

Si una futura validación de staging introduce una regresión en Desarrollo:

1. identificar el último commit de producto conocido-bueno y demostrar su evidencia/gates;
2. revertir únicamente el delta causal en `Desarrollo` mediante un commit normal, sin reescribir historial;
3. volver a ejecutar los gates causales afectados;
4. verificar nuevamente el runtime de staging y `/api/health/ready`;
5. actualizar receipt y control-plane con readback.

Este runbook no autoriza rollback, deploy ni modificación de producción.

## 8. Documentación operativa y task ledger

`CHANGELOG_AI.md` y el `TASKS.md` de la raíz existen en el árbol vigente y son bitácoras históricas append-only; no son fuentes machine-readable del estado operativo actual. El estado operativo vigente permanece en `COLA` y `CONFIG`.

La reconciliación append-only de `TASKS.md` quedó verificada sobre `464127d1729dbb4950e67a8aca84d744efd6f18d..7aeab0ecb4fc6fb7f0a5fdd452c2ad2dcb8420ec`: único archivo afectado, `additions=15`, `deletions=0`, tras recuperación same-run de intentos rechazados.

La reconciliación append-only de `CHANGELOG_AI.md` quedó verificada mediante el writer temporal guardado por blob exacto `bccafe6049b184a8aa1116ce04dc1b14364f7a47`: `new=old bytes exactos + bloque aditivo`, roundtrip de prefijo/sufijo exacto, único archivo afectado, `additions=8`, `deletions=0`, workflow run `35278585466` en `success`; el writer temporal fue retirado inmediatamente en `0b7dc9f85e077a46b71672531fccb924458725fd`.

REVIEW_FIRST final fresco de Supervisión :12 quedó materializado en `344b284fbab334582742967a7de7d1e861cead60`, con `P0=0`, `P1=0`, equivalencia de producto demostrada y receipt final como única acción de cierre pendiente.

## 9. Checklist final N8.13.H

- [x] Dependencia `N8.13.G` LISTO con receipt fresco y readback.
- [x] REVIEW_FIRST current-standard de DOC_CERT: PASS.
- [x] DoD documental material cubierto.
- [x] Runbook y rollback documentados.
- [x] Evidencia runtime de Desarrollo consolidada.
- [x] Comparabilidad con producción demostrada mediante inspección read-only.
- [x] Gates causales terminales y exitosos.
- [x] Equivalencia exacta de árboles de producto demostrada.
- [x] `P0=0`, `P1=0`.
- [x] Sin write en `main`, producción, PR #2, secretos, DNS ni certificados.
- [x] `TASKS.md` append-only hard verify con `additions>0` y `deletions=0`.
- [x] `CHANGELOG_AI.md` append-only hard verify con blob/prefijo exacto, `additions>0` y `deletions=0`.
- [x] REVIEW_FIRST final sobre el HEAD documental resultante.
- [ ] Receipt final y write/readback del control-plane.

## 10. Sucesión

Con el receipt final de `N8.13.H` y su readback, `N8.13` queda certificado bajo estándar actual y el siguiente punto dependency-valid es `N8.14.A`. Hasta ese write/readback final, `N8.14.A` permanece retenido por dependencia.
