# N9.1 — Release Candidate — certificación de candidato y feature freeze

**Autoridad:** `docs/VAEP_AUTHORITY.md`  
**Rama operativa:** `Desarrollo`  
**Candidato fijado:** `4eadd208322e8f19d84d669fffc70a9de85ba567`  
**Estado de este documento:** evidencia material para `N9.1.H`; el cierre requiere REVIEW_FIRST final, receipt y write/readback.

## Identidad del Release Candidate

La identidad del candidato se fija por SHA inmutable de `Desarrollo`:

```text
N9.1-RC-CANDIDATE_SHA=4eadd208322e8f19d84d669fffc70a9de85ba567
```

Ese SHA corresponde al cierre certificado `GATE-N8`. Los commits posteriores creados durante `N9.1.A-G` son evidencia de auditoría/receipts; la revisión de drift no encontró cambios en `backend/`, `frontend/` ni migraciones respecto del candidato fijado.

No se crea ni se modifica una release productiva, no se mueve `main` y no se mergea PR #2. El candidato es reproducible por SHA y permanece separado de cualquier autorización de producción.

## Feature freeze

Desde este punto, para el alcance `N9.1`:

- no se aceptan nuevas features de dominio, base de datos, backend/API o frontend/UI sobre el candidato fijado;
- sólo puede abrirse el candidato por un gap causal de certificación, P0/P1 o incumplimiento material detectado;
- cualquier cambio de producto invalida la equivalencia vigente, obliga a fijar un nuevo candidate SHA y a reejecutar las pruebas/gates aplicables antes de volver a certificar;
- cambios exclusivamente de evidencia no cambian la identidad funcional del candidato, pero deben demostrar equivalencia explícita.

## Revalidación A-G

- `N9.1.A PRE`: scope, fuera de alcance, riesgos, aceptación, rollback y estrategia de pruebas certificados; PR #2 verificado `OPEN + DRAFT`; main/Producción permanecen no autorizados.
- `N9.1.B DOMAIN`: N/A grounded; no entidad/invariante/contrato de dominio requerido por el freeze.
- `N9.1.C DB_MIG`: N/A grounded; no schema/persistencia/migración/backfill requerido.
- `N9.1.D BACKEND_API`: N/A grounded; no servicio/DTO/API nuevo requerido.
- `N9.1.E FRONTEND_UX`: N/A grounded; no UI nueva requerida.
- `N9.1.F SEC_AUDIT`: sin nueva implementación; controles heredados verificados, sin bypass ni secretos.
- `N9.1.G TEST_CI`: PASS por equivalencia exacta del producto y cobertura causal ya certificada.

## Pruebas y gates causales

```text
security:       run 35258962286 = SUCCESS
quality:        run 35258969104 = SUCCESS
backup/restore: run 35223693868 = SUCCESS
performance:    run 35219847763 = SUCCESS
GATE-N8:        LISTO / receipt 4eadd208322e8f19d84d669fffc70a9de85ba567
P0=0
P1=0
```

No se reejecutó CI costoso sin causa porque `N9.1` no produjo delta de producto; `REVIEW_BEFORE_EXPENSIVE_CI` y la equivalencia explícita hacen no causal un rerun idéntico.

## Rollback del candidato

No existe deploy en `N9.1`. Si el candidato queda invalidado, se mantiene `main` y Producción intactos, se reabre únicamente el scope causal, se corrige en `Desarrollo`, se fija un nuevo SHA candidato y se recertifican las pruebas/gates afectadas. No se reescribe historial.

## Seguridad

- `main`: no tocado y congelado.
- PR #2: permanece abierto y Draft; sin merge ni auto-merge.
- Producción: no tocada y no autorizada.
- Secretos, DNS, certificados y datos productivos: no tocados.

## Condición de cierre

`N9.1.H` y el parent `N9.1` sólo pueden pasar a `LISTO` después de reconciliar de forma estrictamente append-only `TASKS.md` y `CHANGELOG_AI.md`, verificar prefijo histórico/additions-only, retirar cualquier writer temporal, ejecutar REVIEW_FIRST final con `P0=0/P1=0`, persistir receipt y completar write/readback de `COLA`, `CONFIG`, `CONTROL_TOWER` y `PLAN_MAESTRO`.
