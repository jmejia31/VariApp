name: N8.20.F - Trigger Aiven DEV Proof

# Recovery iteration 5: dispatch after effective project-membership proof fix.
# N8.8.A current-standard revalidation trigger: 2026-09-17T12:53:00Z.
on:
  push:
    branches:
      - dev
    paths:
      - '.github/workflows/n8-20-f-trigger.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch-proof:
    name: Dispatch exact DEV-scoped Aiven proof
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Dispatch N8.20.F proof on DEV
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh workflow run n8-20-f-aiven-capabilities-proof.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref dev \
            -f aiven_project=solqaryn \
            -f aiven_service=solqaryn-mysql \
            -f persist_evidence=true
          echo 'N8.20.F proof dispatch requested; no secret values were read or printed.'
