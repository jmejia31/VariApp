name: VAEP N9.3.H byte-safe closure append (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired after successful N9.3.H byte-safe append."
