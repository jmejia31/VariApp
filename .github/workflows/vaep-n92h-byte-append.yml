name: VAEP N9.2.H byte-safe CHANGELOG append (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "N9.2.H append writer retired; read-only."
