name: VAEP Checkpoints Paused For J1-J6 Cutover
on: {workflow_dispatch: {}}
permissions: {contents: read}
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo CHECKPOINTS_PAUSED_DURING_ATOMIC_J1_J6_CUTOVER; exit 0'
