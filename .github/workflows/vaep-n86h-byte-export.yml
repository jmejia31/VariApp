name: VAEP Byte Export Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired writer guard
        run: echo 'Historical byte-export writer retired after N8.10.H safe append; no repository writes are permitted.'
