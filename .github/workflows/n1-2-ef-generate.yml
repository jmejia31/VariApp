name: ERP-N1.2 - Generar artefactos EF Almacenes

on:
  push:
    branches: [Desarrollo]
    paths:
      - 'backend/src/Infrastructure/Persistence/Configurations/AlmacenConfiguration.cs'
      - '.github/workflows/n1-2-ef-generate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  generar-ef:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restaurar solución
        run: dotnet restore InventoryApp.sln
      - name: Instalar dotnet-ef
        run: dotnet tool install --global dotnet-ef --version 8.0.8
      - name: Generar migración y snapshot EF
        run: |
          set -euo pipefail
          dotnet ef migrations add N1_2_AlmacenPersistencia \
            --project src/Infrastructure/InventoryApp.Infrastructure.csproj \
            --startup-project src/API/InventoryApp.API.csproj \
            --context AppDbContext \
            --output-dir Migrations
          dotnet ef migrations has-pending-model-changes \
            --project src/Infrastructure/InventoryApp.Infrastructure.csproj \
            --startup-project src/API/InventoryApp.API.csproj \
            --context AppDbContext
          cd ..
          mkdir -p /tmp/n12-ef
          git status --porcelain -- backend/src/Infrastructure/Migrations | cut -c4- > /tmp/n12-files.txt
          test -s /tmp/n12-files.txt
          while IFS= read -r file; do
            mkdir -p "/tmp/n12-ef/$(dirname "$file")"
            cp "$file" "/tmp/n12-ef/$file"
          done < /tmp/n12-files.txt
          cp /tmp/n12-files.txt /tmp/n12-ef/GENERATED_FILES.txt
      - name: Publicar artefactos EF
        uses: actions/upload-artifact@v4
        with:
          name: n1-2-almacenes-ef-generated
          path: /tmp/n12-ef
          if-no-files-found: error
          retention-days: 3
