name: VariApp CI

# Durante el desarrollo activo, el CI solo se ejecuta mediante solicitud manual
# o cuando se actualiza explícitamente un marcador de checkpoint. Los commits
# ordinarios no generan ejecuciones ni correos repetitivos.
on:
  workflow_dispatch:
  push:
    branches:
      - agent/mejoras-variapp
    paths:
      - .github/checkpoints/**

permissions:
  contents: read

concurrency:
  group: variapp-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend build and tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
          cache: true
          cache-dependency-path: backend/**/*.csproj

      - name: Restore
        run: dotnet restore InventoryApp.sln

      - name: Build
        run: dotnet build InventoryApp.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test InventoryApp.sln --configuration Release --no-build --logger "trx;LogFileName=tests.trx"

  frontend:
    name: Frontend production build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build production
        run: npm run build:prod
